# 도구 목록 — Rpi5 에 있는 자작 도구와 외부 공식 도구

> 무엇이 있고, 무엇을 하며, **실제 시연 프로그램(런타임)과 어디가 다른지**를 한 곳에 모은 파악 문서다(2026-09-24 작성).
> 런타임 = `run_demo.sh` → `safety_console.py` → `camera_thread.py` → `fsm.py` 로 이어지는 **실제 시연 프로그램**이다. 런타임 모듈 자체는 도구가 아니라서 이 목록에 없다.
>
> - **채운 방법** — 각 파일의 머리말·코드, 런타임 대조 감사(`조사/런타임-도구-대조-20260924.md`), 저널 §12.73-(5). **모르는 칸은 「미확인」**으로 둔다.
> - ✋ **상태(현행·보관·폐기 후보)는 Claude 의 판단이다** — 지울지·옮길지는 사용자가 이 목록을 보고 정한다. 이 문서는 아무것도 옮기거나 지우지 않았다.
> - 수정일 = 그 파일의 마지막 git 커밋 날짜.

## 읽는 법

| 칸 | 뜻 |
|---|---|
| 상태 | **현행** = 지금 작업에 쓴다 · **보관** = 목적을 다했거나 가끔 쓴다(동작은 함) · **폐기 후보** = 대체됐거나 지금 구성에서 못 쓴다(이유 병기) |
| 실제 시연과 다른 점 | 이 도구의 결과가 실제 시연 프로그램의 결과와 달라질 수 있는 이유. 「같음」 = 런타임 함수를 그대로 불러 쓴다 |
| 인용 조건 | 이 도구가 낸 수치를 문서에 옮길 때 **함께 적어야 하는 조건** |

## 1. 자작 도구

### 1.1 촬영·수집

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/bench_detector.py` | Hailo 실추론 벤치 — FPS·탐지율·안정성 측정 **겸** raw PNG 촬영(`--save-raw`) | 현행 | 🔴 추적에 **클래스당 1개 규칙 없음**(detection_log 39% 프레임 중복) · 새 박스를 **곧바로 확정**(런타임 R2 의 두 프레임 확정·오래된 트랙 우선 없음 · 통합문서 §6.3) · 🔴 `--undistort` **기본 꺼짐** · FPS = 순간 FPS **평균**(런타임은 중앙값) · GUI 부하 없음, 대신 창·mp4·PNG 저장 부하 · 「B4↔EMO 오인」 요약은 구조상 항상 0 | FPS: PNG 저장 빈도·`--headless` 여부·평균식 병기 · 검출 수: 중복 트랙 포함 · 세션 manifest 의 `undistort`·`frame_size` 확인 | 2026-09-23 |
| `Demo/test/capture_dataset.py` | 데이터셋 촬영 전용 — 검출 없이 런타임과 같은 그림(반전→보정→회전)만 저장 | 현행 | 방향 처리 같음(보정 맵 없으면 `--force` 없이 멈춤) · 검출·손 없음 | 촬영 도구라 수치 없음 | 2026-09-22 |
| `Demo/test/calib_capture.py` | 해상도별 카메라 보정 파일 촬영·계산(`camera_calibration_<w>x<h>.npz`) | 현행 | 같음(반전 후 계산 — 런타임 적용 순서와 일치) | RMS·덮은 범위는 해상도·판 종류와 함께 | 2026-09-23 |
| `Demo/test/tool_probe.py` | 공구 검출 + 서브작업(찾기·쥠) 점검 · 홀드아웃 촬영(`--save-clean`) | 현행 | 공구 판정 부품은 런타임 그대로(가장 가깝다) · 서브작업 타이머를 워커 준비 뒤 시작 · 버튼 검출 없음 · 매 프레임 H.264 녹화 부하 | 소요 시간은 런타임보다 짧게 나올 수 있음 | 2026-09-22 |
| `Demo/test/holdout_frames.py` | 촬영본에서 라벨링용 프레임 추리기(중복 제거·시간 간격) | 보관 | 해당 없음 | — | 2026-09-13 |
| `Demo/test/dedupe_raw.py` | raw 프레임 pHash 중복 제거(데이터 준비 1단계) | 현행 | 해당 없음 | 해밍거리 기준 병기 | 2026-07-13 |
| `Demo/test/frame_health.py` | 깨진 프레임(h264 디코딩 실패) 후보 찾기 | 현행 | 해당 없음 | 후보일 뿐 — 사람이 확인 | 2026-09-21 |
| `Demo/demo_postprocess.py` | 시연영상 촬영 뒤 규격 맞추기(1인칭 레터박스) | 현행 | 해당 없음 | — | 2026-09-23 |
| `Demo/run_bench_test.sh` | `bench_detector` 바탕화면 런처(조건 슬러그) | 현행 | `bench_detector` 와 같음 | 〃 | 2026-09-23 |
| `Demo/run_scenario.sh` | 시나리오 촬영 — GUI + 화면녹화 동시 시작 | 현행 | 런타임 그대로 띄움 + 녹화 부하 | FPS 는 녹화 부하 포함 | 2026-09-23 |
| `Demo/촬영런처.sh` | 시연영상 촬영 — 영상 3개 동시 녹화 | 현행 | 런타임 + 동시 녹화 부하(여유 없으면 끊김) | 〃 | 2026-09-23 |
| `Demo/run_demo.sh` | 시연 실행 런처 | 현행 | 런타임 그 자체 | — | 2026-08-05 |

### 1.2 성능·환경 측정

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/cam_probe.py` | 촬영 전 ESP32 실측 속도 점검(닿는가가 아니라 빠른가) | 현행 | 10프레임 표본 — 분산이 커 관문으로 쓰지 않는다(측정도구 스킬) | 게이트 값으로 인용 금지 | 2026-07-22 |
| `Demo/test/cam_timing_calc.py` | 센서 클럭·프레임률 계산기(실HW 불필요) | 보관 | 해당 없음(계산값) | 계산값임을 명시 | 2026-09-22 |
| `Demo/test/link_probe.py` | ESP32 링크 품질 한 라운드 측정 | 보관 | 해당 없음 | `link_ctx` 문맥(보드·망) 함께 | 2026-09-04 |
| `Demo/test/link_ctx.py` | 링크 측정 문맥(보드·망·TCP 상태) 기록 | 보관 | 해당 없음 | — | 2026-09-19 |
| `Demo/test/pi_load.py` | 파이 자원 사용률 스냅샷 — 측정이 부하에 오염됐는지 | 현행 | 해당 없음 | — | 2026-08-26 |
| `Demo/test/thermal_probe.py` | 연속 스트리밍 중 발열이 성능을 깎는지 | 보관 | 해당 없음 | — | 2026-08-26 |
| `Demo/test/anim_fps_bench.py` | UI 애니메이션 ON/OFF FPS 비교(G6) | 보관 | 공급 FPS 요동으로 효과가 묻힐 수 있음(ESP32 뿐) | 결과와 함께 한계 병기 | 2026-09-23 |
| `Demo/test/verdict.py` | 최근 벤치 세션의 중단 규칙 판정(클린룸 현장용) | 보관 | 추적 없는 raw 검출 기준 · `CONF_WARN 0.70` 복제 | — | 2026-07-20 |

### 1.3 손·판정(HOI) 분석

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/dwell_probe.py` | 체류·선행시간·사전 감지율(PNG + GPIO 로그) | 현행 | 🔴 **FSM 이 아니다** — 갭메우기 재구현(앞뒤 같은 ROI 일 때만) · 🔴 **추적 없음**(가리면 박스 즉시 사라짐 · 런타임은 5프레임 유지) · 🔴 **버튼 검출 없는 프레임은 시계열에서 뺀다** · 링·정답 ROI 체류도 셈 · 손 모델은 `hoi_probe.run_hand` 복제 | 「실제 시연 사전 감지율」로 인용 금지 — **dwell_probe 값**이라고 쓴다 · 도구끼리 비교만 유효 | 2026-09-23 |
| `Demo/test/hoi_probe.py` | Hailo 손 검출 경로 실증 · `--batch` | 보관 | 모델·blaze 경로·TIP 하드코딩(현재 config 와 같은 값) · 깨끗한 프레임 입력(런타임은 박스가 그려진 프레임) | — | 2026-08-18 |
| `Demo/test/hoi_probe_batch.py` | raw 세션 전량 손 검출 캐시(HOI DB 1단계) | 현행 | 🔴 **추적 없음** · 🔴 손 flag(≥0.2) 필터 없음(zone 프레임 3.8% 가 런타임이면 버려짐) · 팜 임계 `--thresh` 필수(0.5 캐시만 런타임과 일치) | 팜 임계 병기 · 0.2 결과를 런타임 값으로 인용 금지 | 2026-09-23 |
| `Demo/test/hoi_import.py` | HOI 캐시 → `hoi.db`(눌림 1회 = 1행) · y 좌표를 VGA 로 환산 | 현행 | `hoi_probe_batch` 의 차이를 그대로 물려받음 | 〃 | 2026-09-23 |
| `Demo/test/hoi_metrics.py` | 사전 감지율 판정 규칙 **단일 출처** | 현행 | 입력이 추적 없는 raw · `PALM_THRESH=0.5` 고정(런타임 값은 config 밖 blaze 기본값) · `CLIFF_Y=337` 은 VGA 좌표 | 〃 | 2026-09-23 |
| `Demo/test/fsm_sim.py` | `hoi.db` 를 **실제 SafetyFSM** 에 먹여 재생(E2E 오경보·차단율) · `--gate` 관문 | 현행 | FSM 은 같음 · 입력은 추적 없는 raw · 체류 임계를 config 에서 읽음(런타임은 recipe.json — 지금은 둘 다 0.3) · `--gate` 가 `dwell_probe` 재구현·상수에 기댐 · 시뮬레이션 정책 4종 | **시뮬레이션 정책 4종 병기**(수치인용 규칙) · 추적 미반영 | 2026-07-28 |
| `Demo/test/b3emo_score.py` | B3→EMO 자리 기준 채점(rawdet 오프라인) | 현행 | 추적 없음 — 런타임 클래스당 1개 규칙 뒤에는 대부분 걸러질 수 있음(미검증 추정) · 반경 기준 25 하드코딩 | 범위(S1·S2·S6 등)·반경·「rawdet 기준」 병기 | 2026-09-23 |

### 1.4 데이터셋·라벨

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/export_labels.py` | `rawdet_log.csv` → YOLO 라벨 txt(프리라벨) | 현행 | 로그를 만든 모델의 원시 검출(추적 없음) — 오탐도 그대로 라벨이 됨(§12.73-(4)) | 시험 세트에 쓰지 않는다(순환) | 2026-07-21 |
| `Demo/test/upload_roboflow.py` | 이미지 + 프리라벨 Roboflow 일괄 업로드(분할 명시) | 현행 | 해당 없음 · ⚠️ Roboflow 가 JPEG 75 로 재압축(§12.73-(3)) | — | 2026-07-21 |
| `Demo/test/label_audit.py` | 라벨 export 기계 점검(클래스명·조각 박스·배경 비율·장수) | 현행 | 해당 없음 | — | 2026-09-21 |

### 1.5 모델 채점·확인

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/replay_raw.py` | 저장된 raw PNG 에 `.hef` 재생(`--hef` 지정 가능) | 현행 | 검출기 같음 · 「≥0.65 건수」는 검출 단위(런타임 트랙 규칙 아님) · manifest 의 `undistort` 를 확인하지 않음 | 추적 미반영 · 세션 보정 유무 확인 | 2026-07-18 |
| `Demo/test/score_hef.py` | 정답 라벨 대비 `.hef` 정량 채점(mAP 등) | 현행 | 검출기 같음 · 운용점 conf 를 「FSM 이 보는 값」이라 표기하나 검출 단위 | 시험 세트 조건(모조/실콘솔) 병기 | 2026-07-21 |
| `Demo/test/score_lib.py` | 채점 계산(IoU·매칭·AP·혼동행렬) — 자체 테스트 포함 | 현행 | 해당 없음 | — | 2026-07-21 |
| `Demo/test/holdout_score.py` | 공구 모델 홀드아웃 채점(`tool_v3` 대 `tool_v4`) | 보관 | 입력이 PNG — 런타임 공구 워커는 **JPEG q80 재압축본**을 받음 → 도구가 낙관적일 수 있음 | 재압축 차이 병기 | 2026-09-11 |
| `Demo/test/tool_live.py` | 공구 검출 실시간 눈 확인 뷰어 | 보관 | 🔴 왜곡보정 없음 · conf **0.60 하드코딩**(런타임 0.65) · 재압축 없음 | 보고값으로 쓰지 않는다 | 2026-08-26 |

### 1.6 DB·보고

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/db_import.py` | 벤치 CSV → `bench.db` | 현행 | `bench_detector` 의 중복 트랙을 그대로 적재 · 세션의 해상도·보정 여부를 적재하지 않음(VGA/XGA 구분 불가) | 검출 수는 중복 포함 | 2026-07-20 |
| `Demo/test/db_report.py` | `bench.db` → 열람용 HTML(시험판) | 보관 | 해당 없음 | 정본 아님(자체 명시) | 2026-07-18 |
| `Demo/test/slide_charts.py` | 발표용 차트 8종(SVG) | 보관 | 해당 없음 | 차트 수치는 원 절의 조건을 따름 | 2026-08-24 |

### 1.7 음성

| 도구 | 목적 | 상태 | 실제 시연과 다른 점 | 인용 조건 | 수정일 |
|---|---|---|---|---|---|
| `Demo/test/fake_glass.py` | 모의 ESP32 — HW 없이 음성비서 전 구간 실행(tts venv) | 보관 | 실제 마이크 대신 wav 주입(DC 오프셋 흉내) | 모의 입력임을 명시 | 2026-09-06 |
| `Demo/voice/llm_gate.py` | 프롬프트 관문 G2·G3·G4(tts venv) | 보관 | 해당 없음 | 모델 품질 채점 아님(자체 명시) | 2026-09-08 |
| `Demo/voice/make_answers.py` | 정형 답변 wav 사전 합성(tts venv) | 보관 | 해당 없음 | — | 2026-09-08 |
| `Demo/voice/record_voice_demo.py` | 음성비서 시연 촬영(콘솔 없이) | 보관 — ⚠️ 웹캠 제거로 **영상용 녹음 수단 없음** | 공구 임계를 0.30 으로 낮춤(런처) | 공구 임계 병기 | 2026-09-23 |
| `Demo/voice/run_record.sh` | 음성 시연 촬영 런처 | 보관 | 〃 | 〃 | 2026-09-23 |
| `Demo/voice/gate_check.sh` | 음성 촬영 직전 관문 러너(HW 필요) | 보관 | 해당 없음 | — | 2026-09-07 |
| `Demo/run_voice.sh` | 음성비서 데몬 실행(tts venv) | 보관 | 런타임과 별개 프로세스 | — | 2026-09-06 |

### 1.8 펌웨어·하드웨어

| 도구 | 목적 | 상태 | 비고 | 수정일 |
|---|---|---|---|---|
| `arduino/camera_stream_tcp/` | **메인 카메라 펌웨어**(XGA · 부팅 노출 9ms) — `flash_esp32.sh` 가 굽는 대상 | 현행 | 구운 것과 저장소가 같은지는 굽기 기록으로 확인 | 2026-09-23 |
| `arduino/glass_audio/` | **서브 보드 오디오 전용**(마이크·스피커 · 포트 8889·8890) — 부팅 로그 첫 줄 `=== 서브 글라스 — 오디오 전용` | 보관(음성) | 2026-09-06 23:10 보드 분리 뒤 09-07 실HW 통과가 이 펌웨어로 났다(`8eb027e` 은 이것만 고침). ✅ **서브 보드(`…5E:40`)에 구워져 있음을 확인**(2026-09-24 부팅 로그 — PSRAM 버퍼·마이크 I2S0/PDM·스피커 I2S1 초기화 OK) | 2026-09-19 |
| `arduino/glass_voice/` | 한 보드 **통합**(카메라 + 음성) — 보드 분리 전 안(09-06 21:56). 부팅 로그 `=== TCP Streaming Start ===` | 보관 | 09-07 수정은 두 펌웨어에 같이 반영됨 · 통합문서 통신 초기화 도식이 이 파일을 정본으로 가리킨다(810행) | 2026-09-19 |
| `arduino/console_interlock/` | 아두이노 인터락(릴레이 5채널) | 현행 | CH5 실동작 미결(통합문서 🔓) | 2026-09-05 |
| `arduino/flash_esp32.sh` | ESP32 펌웨어 굽기(포트 자동 탐지 → IP 기록) | 현행 — ⚠️ 위험 | 🔴 포트를 `ls /dev/ttyACM* \| head -1` 로 골라 **보드 두 대면 엉뚱한 보드에 굽는다**(저널 기록) — 시리얼번호 확인 필수 | 2026-09-19 |
| `arduino/read_esp32_ip.sh` | 시리얼 로그에서 IP 읽어 `.camera_ip` 기록 | 현행 | 촬영 직전 필수(Rpi5/CLAUDE.md) | 2026-09-19 |
| `arduino/update_ip.sh` | 바탕화면 「ESP32-IP갱신」 바로가기용 래퍼 — `read_esp32_ip.sh` 를 부르고 창을 붙잡아 둔다 | 현행 | 겹침 아님(래퍼) · 굽지 않는다 | 2026-07-13 |
| `arduino/cam_audio_batt_test/` · `cam_probe_i2c/` · `i2s_batt_test/` · `i2s_tone_test/` · `mic_test/` · `mic_speaker_test/` · `relay_click_test/` · `relay_idle/` | 부품·배선 시험 펌웨어 | 보관 | 일회성 시험 — 재시험 때 씀 | 2026-07~09 |

### 1.9 자가 테스트

- `Demo/selftest/test_*.py` **33개** + `Demo/test/test_gpio_available.py` 1개 — 도구가 아니라 **검사**다. 전부 = `python3 Demo/selftest/run_all.py`(일괄 러너 · 종료 코드 0 = 실패 없음 · `test_voice_tts` 는 전용 파이썬 `~/env/tts/.venv/bin/python` 으로 알아서 · 없으면 건너뜀) · 하나만 = 그 파일을 직접 실행(CLAUDE.md 명령 표). `test_gpio_available.py` 는 실제 GPIO 를 보므로 러너에 들지 않는다.
- `Demo/test/score_lib.py` 는 자체 테스트를 품은 계산 모듈(1.5).

### 1.9-b 참고용 구버전 — `original/` (11개)

- `calibrate_camera.py` · `data_collector.py` · `main_original.py` · `plot_logs.py` · `provision_wifi.py` · `view_stream_tcp.py` · `view_stream_tcp_test_usb.py` · `view_stream_test.py` · `view_stream_test_custom.py` · `wifi_dialog.py` · `yolo_hailo_tcp.py` — 수정일 2026-05-20.
- 상태 = **보관(참고용)** — Rpi5/CLAUDE.md 「참고용 구버전」. 실행 대상이 아니다. 개별 파일의 쓰임은 **미확인**.

### 1.9-c 폐기한 도구 (2026-09-24 삭제 · 사용자 승인)

| 도구 | 이유 |
|---|---|
| `Demo/test/autolabel.py` · `review_labels.py` | 색 기반 자동 라벨 미채택(EMO↔B3 오분류) · 뒤의 것은 그 결과 검토용 |
| `Demo/test/run_tool_infer.py` | 외부 공구 데이터셋 모델을 쓰지 않기로 함(데이터 확보 설계 §2) |
| `arduino/camera_stream/`(펌웨어 + `latency_test.py`·`view_stream.py`) | `camera_stream_tcp` 로 대체 · 굽기 스크립트는 `_tcp` 만 씀 |
| `arduino/hello_test/` · `wifi_test/` | 2026-05 초기 동작 확인용 |
| `scripts/serial_monitor.py` · `yolo_camera.py` | 2026-05 · 첫 번째 포트를 고름(시리얼번호 식별 규칙과 어긋남) · 제거된 USB 웹캠·없는 `~/Downloads/best.pt` 경로 |
| `Demo/check_model.py` | `console_v1.pt`(PyTorch) 모델 계약 검증 — v1 시절 · 현행은 `console_v2.hef`. ⚠️ **제출한 제작설계서 35쪽 「프로그램 목록 ① 인식」에 카드로 실려 있다** → 다음 판에서 뺀다(`~/project-docs/hanium-장려상/손볼목록.md`) |

- 되살리기 = `git checkout <삭제 커밋>^ -- <경로>`(Rpi5 저장소). `camera_stream/` 에 남아 있던 `wifi_credentials.h` 는 공용 `arduino/wifi_credentials.h` 와 내용이 같은 사본이라 함께 지웠다.

### 1.10 런타임과 같은 부품을 그대로 쓰는 곳 (믿어도 되는 부분)

| 런타임 부품 | 불러 쓰는 도구 |
|---|---|
| 검출기 `detector.create_detector`(640 늘림·색 순서·임계·HEF NMS) | `bench_detector` · `replay_raw` · `score_hef` |
| 방향 처리 `frame_orient`(반전·보정 선택·회전·링 크기) | `bench_detector`(회전·보정) · `tool_probe` · `capture_dataset` · `calib_capture` · `dwell_probe` · `hoi_probe_batch` · `hoi_import` · `b3emo_score` |
| 버튼 구역 판정 `roi_zones.zone_at_point` | `dwell_probe` · `hoi_probe_batch` |
| 순서 판정기 `SafetyFSM` | `fsm_sim` |
| 손 검출 `HandTracker` | `bench_detector --hand` · `tool_probe` |
| 공구 판정 `ToolGate`·`ToolState`·`SubTask` | `tool_probe` |
| 사전 감지 창 판정 `hoi_metrics.capability_hit` | `dwell_probe` · `fsm_sim --gate` |
| 🔴 **추적 `camera_thread._update_tracks`(클래스당 1개 포함)** | **아무 도구도 쓰지 않는다** — `bench_detector` 는 따로 구현, 분석 도구들은 추적 없음 |

## 2. 외부 공식 도구

| 도구 | 버전 | 어디에 쓰나 |
|---|---|---|
| `arduino-cli` (`~/bin`) | 1.4.1 | ESP32·아두이노 펌웨어 컴파일·업로드(`flash_esp32.sh`) |
| `esptool` (`~/.local/bin`) | 5.3.1 | ESP32 플래시 8MB 백업·복원·비교(`~/lab/esp32-link/RESTORE.md`) |
| `hailortcli` · HailoRT(파이썬 `hailort`) | 4.23.0 | Hailo-8 NPU 추론(버튼·손 모델) |
| `hailo-rpi5-examples` (`~/env`) | — | **쓰는 곳 없음** — 04-06 이후 미사용(2026-08-18 홈 정리 조사 · 1.3GB) |
| OpenCV(시스템 파이썬) | 4.10.0 | 디코드·보정·그리기 전반 |
| PyQt6 (+ PyQt5 설치됨) | 6.11 | 런타임 GUI · PyQt5 는 **코드에서 쓰는 곳 없음**(import 0건) |
| numpy · pyserial · gpiozero · lgpio | 2.2.4 · 3.5 · 2.0.1 · 0.2.2 | 수치 · 시리얼(인터락·ESP32) · 버튼 GPIO |
| `~/env/rfenv` — ultralytics · torch(CPU) | 8.4.117 · 2.13.0 | 공구 모델 워커(시스템 파이썬엔 없다 · config 주석) |
| `~/env/tts/.venv` — sherpa-onnx | 1.13.7 | 음성 STT·TTS · `test_voice_tts` |
| `~/env/docvenv` — python-docx | 1.2.0 | 라벨링 가이드 `.docx` 생성 전용(`~/lab/labeling-guide/make_guide.py`) |
| `ffmpeg` · `ffprobe` | 7.1.3 | 녹화 인코딩·영상 확인 |
| `ollama` | 0.21.2 | LLM 서버(음성비서 · sop-pi-2 쪽 운용은 통합문서 §9.5) |
| `rpicam-hello` · `v4l2-ctl` | rpicam-apps 1.12.0 | **쓰는 곳 없음**(카메라는 ESP32) |
| `arecord` · `aplay` · `espeak-ng` | 시스템 | 오디오 녹음·재생·합성 확인 |
| `pdftotext` | 시스템 | 제출 PDF 읽기·대조 |
| `chromium` · `xdotool` · `yt-dlp` · node/npm | 시스템 | chromium = 제작설계서 SVG→PNG 렌더(`~/project-docs/hanium-장려상/제작설계서/도구/svg2png.py`) · xdotool = 쓰는 곳 없음(자동 키 주입 시도는 폐기) · yt-dlp = 쓰는 곳 없음 · node/npm = 미확인 |
| 손 모델 BlazePalm·BlazeHandLandmark + blaze 코드 | 코드 `AlbertaBeef/blaze_app_python` 커밋 `ef01509`(2026-01-15) · 모델 `blaze_hailo8_models.zip` 의 `palm_detection_lite.hef`·`hand_landmark_lite.hef`(파일 2024-08-28) | 저장소 밖 `~/lab/hoi/` — 런타임이 쓰는 파일(`hand_tracker.py:124-125`) · 팜 임계 0.5 가 blaze 기본값에 있다 |
| Roboflow (웹 + MCP) | — | 라벨링·데이터셋 관리(도구 확정 전) · 올린 이미지를 JPEG 75 로 재압축 |

## 3. 파악하며 보인 것 (조치는 하지 않음)

- **추적을 런타임과 같게 쓰는 측정 도구가 하나도 없다**(1.10 마지막 줄). 사전 감지율·차단율·검출 수가 실제 시연과 다를 수 있는 가장 큰 이유다(저널 §12.73-(5)).
- **역할 겹침** — 촬영이 `bench_detector --save-raw` · `capture_dataset` · `tool_probe --save-clean` 세 곳에 있다. (IP 갱신 둘은 겹침이 아니라 래퍼 관계로 확인됐다.)
- **config 를 따르지 않고 값을 복제한 곳** — 클래스 이름·박스 색·손끝 번호(TIP=8)·blaze 경로·`CONF_WARN 0.70`·`tool_live` conf 0.60·프레임 최대 크기 512KiB.
