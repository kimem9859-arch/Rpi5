# GUI·흐름 영역 버그 점검 — Rpi5/Demo (읽기 전용)

- 대상: safety_console · main · overlay · overlay_menu · overlay_result · anim · theme · precheck · session_stats · state_publisher · sub_task · recipe(+json) · demo_recorder · demo_ffmpeg
- 함수목록.md §4(이미 알려진 것)는 빼고 적었다. #10 만 §4.1-6 의 **새 경로**라 따로 표시했다.
- 검증 방법
  - 코드 경로를 끝까지 따라갔다(확실 = 따라감).
  - 추가로 **헤드리스 재현**을 했다(표의 「재현✓」). scratchpad 의 `flow_probe.py` · `flow_probe2.py` 가 `SafetyConsole` 을 `QT_QPA_PLATFORM=offscreen` 으로 만든다. `detector` 모듈은 가짜로 바꿨고 손·공구·GPIO·인터락은 config 로 껐다. 카메라는 127.0.0.1 이라 즉시 거절된다. 로그·상태 파일은 scratchpad 에만 썼다. 하드웨어·Hailo·실제 창은 쓰지 않았고 저장소도 건드리지 않았다(git status 깨끗).
  - `overlay_probe.py`·`qss_probe.py` 는 위젯을 오프스크린으로 그려 픽셀을 읽는다.

| # | 심각도 | 확신 | 파일:줄 | 문제 | 시나리오 (단계 → 잘못된 결과) | 근거 |
|---|---|---|---|---|---|---|
| 1 | 높음 | 확실 · 재현✓ | safety_console.py:1306-1320 (BLOCK 분기), 1144-1159 `_finish_sub`, fsm.py:286-288 | **EMO(또는 BLOCK) 중에도 서브 작업이 취소되지 않는다.** EMO 해제로 기대단계가 1로 리셋된 뒤, 그 전에 누른 버튼이 서브 완료 시점에 FSM 에 자동으로 커밋된다 | ① 작업 시작 → B1·B2 완료 → B3 누름(10초 「전극 온도 하강」 시작) → 3초에 EMO → 2초 뒤 EMO 복귀·「차단 해제」 → 기대단계=1, 서브는 계속 돈다 → 10초에 서브 완료 → `_commit_button("B3")` → B3 는 1단계의 오답 → **가짜 BLOCK**. 인터락이 버튼 GND 를 끊는다. `_last_button`=EMO 라서 위반 기록도 안 남는다(재현: `state=BLOCK step=1 violations=[]`). ② 같은 일을 B1 서브 중에 하면 EMO 뒤인데도 **1단계가 다시 누르지 않아도 자동 완료**된다(재현: step 1→2). B2(공구) 서브도 공구를 쥐는 순간 ①과 같다 | `_on_fsm_state` 의 서브 정리(`_sub_timer.stop`·`_sub=None`·`_end_tool_scan`)는 **IDLE 분기에만** 있다(1340-1349). BLOCK 동안에도 `_tick_sub`·`_on_tool` 은 계속 돈다. EMO 를 누르고 몇 초 안에 복귀하는 것은 시연에서 자연스러운 동작이고, 서브는 10초다 |
| 2 | 높음 | 확실 · 재현✓ | safety_console.py:1121-1134 `_update_sub_view` | **다른 공구 경고가 BLOCK 배너를 200ms 안에 덮는다.** 해제 버튼이 사라진다. 공구를 내려놓으면 배너와 발광이 **통째로 사라지는데** FSM 은 계속 BLOCK 이다 | B2 공구 서브 중 드라이버(오답)를 쥠 → 「다른 공구입니다」 → B3 오조작(또는 EMO) → BLOCK 배너 → 다음 틱에 `alert.mode("block") != "tool"` 이라 `show_wrong_tool` 이 다시 그린다 → 「차단 해제」 버튼이 없다(재현: `release btn visible False`). 드라이버를 놓음 → `hide_all`·`glow None` → **화면에는 아무 경고도 없고 FSM 은 BLOCK, 릴레이는 차단 중.** 되돌릴 방법은 메뉴 → 작업 초기화뿐이다 | 공구 경고 분기는 FSM 상태를 보지 않고 `alert.mode` 만 비교한다. 오답 공구 경고가 새로 뜰 때마다 `_notify` 와 `tool_grasped(False)` 도 한 번씩 더 쌓인다 |
| 3 | 높음 | 확실 · 재현✓ | safety_console.py:1121-1134, 1144-1159 | **#2 와 같은 충돌이 WARNING 에서도 난다.** 순서 경고 배너가 공구 배너로 바뀌어 「경고 해제」가 사라진다. 올바른 공구로 바꾸면 배너 없이 WARNING 에 갇힌다 | B2 서브 중 드라이버를 쥔 손이 B3 박스에 0.3초 머묾 → WARNING(order) → 200ms 뒤 공구 배너(재현: `alert=tool`) → 렌치로 바꾸고 10초가 참 → 서브 완료 커밋은 WARNING 이라 무시된다(§4.1-6) → **서브는 사라지고 단계는 그대로다. FSM 은 WARNING 인데 배너·해제 버튼이 없다**(재현: `state=WARNING sub=N alert visible False`). 오답 버튼을 눌러 BLOCK 으로 보내거나 초기화하는 수밖에 없다 | 원인은 #2 와 같다 |
| 4 | 중간 | 확실 · 재현✓ | safety_console.py:977-979 | **BLOCK·WARNING 중에 정답 버튼을 누르면 서브 작업이 시작되고**, 해제가 서브보다 먼저 끝나면 그 눌림이 단계 완료로 인정된다. fsm.py:223 「차단 중 입력 무시」와 어긋난다. 서브가 없는 B4 는 같은 상황에서 무시되어 단계마다 동작이 다르다 | 1단계에서 B3 오조작 → BLOCK → (키보드 `1`, 또는 인터락이 fallback 이라 버튼 GND 가 안 끊긴 경우) B1 누름 → 게이지 시작(재현) → 차단 해제 → 10초 뒤 **2단계로 진행**. 인터락이 연결돼 있으면 BLOCK 이 물리 버튼 GND 를 끊으므로(console_interlock.ino IN5) 키보드나 fallback 에서만 일어난다 | 조건이 `self.fsm.state != State.IDLE` 뿐이다. BLOCK·WARNING 을 거르지 않는다 |
| 5 | 중간 | 확실 · 재현✓ | fsm.py:279-290 · safety_console.py:1300-1351 · session_stats.py:125-150 | **IDLE 에서 EMO(기동 시 EMO 가 HIGH 인 경우 포함) → 해제하면 「작업 시작」 없이 PROCESS_RUN 이 된다.** BLOCK 분기가 `_sync_cta_visibility` 를 부르지 않아 가운데 「▶ 작업 시작」이 진행 중에도 떠 있다. 통계가 시작되지 않아 결과창이 틀린다 | 1회 완주 → 결과창 닫기 → E(EMO 시연) → 해제 → PROCESS_RUN(재현: `cta visible True`, `stats.running False`) → B1~B4 진행 → 결과창에 누른 순서 「**B1→B2→B3→B4→B1→B2→B3→B4**」, 「**총 0초**」, 프레임 0. 이전 회차에 위반이 있었다면 판정도 「위반 있음」으로 남는다. 기동 때 EMO 가 HIGH 면 gpio 생성자가 곧바로 EMO 를 쏘는 경로(gpio_input.py:78-80)도 같다 | `finish()` 는 `_started` 만 지우고 `_steps`·`_violations`·`_interlocks` 는 지우지 않는다. `step_done`·`violation`·`interlock` 은 running 을 보지 않는다. session_stats.py:144-149 주석도 이 경로를 알고 있으나 막는 것은 `frame()` 하나다 |
| 6 | 중간 | 확실 · 재현✓ | safety_console.py:894-919 vs 874-879 | **시연용 `t`(공구 쥠 처리)가 다음 공구 결과에 덮인다.** `ToolState` 는 계속 search 상태라서 약 1초 뒤 `_on_tool` 이 `set_tool(None)` 을 한다 | 공구 워커가 도는 중 B2 서브 3초에 `t` → tool_ok=True → 다음 결과(공구 미검출) → tool_ok=False(재현). 독스트링의 「10초가 차는 순간 자동 진행」이 지켜지지 않는다. 공구 판정 주 규칙이 지금 모델에서 안 도는 상황(§4.1-3)이라 `t` 를 쓸 일이 많다 | `_sim_tool_grasped` 는 `self._sub` 만 바꾸고 `self._tool_state` 는 건드리지 않는다. 10초가 지난 뒤 누르면 곧바로 진행되므로 그때는 괜찮다 |
| 7 | 중간 | 확실 · 픽셀 확인 | overlay.py:425-456 `GlowFrame` | **경고·차단 발광 테두리가 한 번도 그려지지 않는다.** GlowFrame 은 파이썬 QWidget 하위 클래스인데 `WA_StyledBackground` 가 없어서 QSS border 를 무시한다 | WARNING·BLOCK 진입 → `glow.set_level("block")` → 화면에 빨간 테두리가 없다. 오프스크린 렌더로 테두리 자리 픽셀을 읽으면 `#808080`(배경 그대로)이고 기대값은 `#ff5252` 다. `PANEL_BACKGROUND` 켬·끔 모두 같다 | `qss_probe.py`: 하위 클래스 + 속성 없음 → 그리지 않음, 속성 True → 그림. overlay.py:102-104 주석도 같은 현상을 적어 두었다 |
| 8 | 중간 | 확실 · 픽셀 확인 | overlay.py:101-119, 486-488, 571 | **경고·차단 배너의 진한 판·색 테두리·차단 맥박이 기본 설정(PANEL_BACKGROUND=False)에서 안 그려진다.** AlertBanner 가 `_Panel.apply_theme` 을 물려받아 `WA_StyledBackground` 를 끄기 때문이다. 설정의 「공정 단계 패널 배경」을 바꾸면 **배너도 같이** 바뀐다. 배너가 떠 있을 때 테마·배경을 바꾸면 `_paint` 가 입힌 QSS 가 일반 패널 QSS 로 덮여 색 테두리가 사라진다 | 기동(기본값) → BLOCK → 배너 글자만 영상 위에 뜬다. 판·빨간 테두리·맥박이 없다. 픽셀: 배경 `#808080` 그대로. PANEL_BACKGROUND=True 면 `#0d0d0d` 판과 `#ff5252` 테두리가 그려진다 | `overlay_probe.py` 두 번 실행(off/on) |
| 9 | 중간 | 가능성 · 재현✓(FSM 입력 흉내) | safety_console.py:1144-1159 + fsm.py:201-213 | **서브 대기 중 손끝이 방금 누른 버튼 박스 안에 있으면, 자동 진행 순간 그 버튼이 오답이 되어 0.3초 뒤 가짜 WARNING 이 뜬다** | B1 누름 → 10초 대기 동안 손가락을 B1 위에 둠(대기 중에는 B1 이 정답이라 조용하다) → 자동 진행으로 기대=B2 → B1 체류 0.35초 → WARNING 「순서가 다릅니다」(재현). 자동 진행 시각을 작업자가 모른다는 점이 문제를 키운다 | FSM 에는 「방금 완료한 버튼」 예외가 없다. 서브가 없던 시절에는 눌림과 동시에 올라가 손이 떠날 때 생길 수 있는 일이었다. 실제 운용에서 손을 얹어 두는지는 실기동으로 확인해야 한다 |
| 10 | 중간 | 확실 | safety_console.py:1139-1159 | **(§4.1-6 의 새 경로)** 서브 진행 중 WARNING 이 나면, 게이지가 「완료」까지 찬 뒤 `_finish_sub` 가 서브를 지우고 커밋하지만 FSM 이 무시한다. 로그·알림 없이 단계 진행이 사라진다 | B1 서브 중 손이 B2 박스에 머묾 → WARNING → 해제하지 않은 채 10초 경과 → 게이지 사라짐, 단계는 1 그대로 → 해제 뒤 B1 을 다시 누르고 10초를 또 기다려야 한다. 화면에는 이유가 없다 | §4.1-6 은 사람이 WARNING 중에 누르는 경우다. 여기는 **자동 진행**이 조용히 버려지는 경우다 |
| 11 | 중간 | 확실 | demo_recorder.py:74-79 · safety_console.py:1443-1446 · config.py:235-238 | **시연촬영 1인칭 크기가 늘 폴백 480×640 이다.** sink 를 붙인 직후 `start()` 가 `_latest` 를 읽는데 그 사이에 프레임이 들어올 틈이 없다. 카메라 펌웨어가 XGA(768×1024, 2026-09-23 전환)라서 제출용 1인칭 영상이 줄어서 담긴다 | SOP_DEMO_CAPTURE=1 → 첫 프레임 → `_start_demo_capture` → `set_frame_sink` 다음 줄에서 `start()` 가 `latest=None` → `DEMO_FPV_SIZE` 를 쓴다. config 주석 「첫 프레임이 있으면 그 크기」는 실제로 일어나지 않는다 | camera_stream_tcp.ino:366 `FRAMESIZE_XGA`. `_feed` 가 매 프레임 `cv2.resize`(demo_recorder.py:119-120). 촬영메타.json 도 480×640 으로 적혀 후처리와는 맞는다 |
| 12 | 중간 | 가능성 | safety_console.py:1122-1134 · tool_state.py:86-87 | **공구 검출이 깜빡일 때마다 「다른 공구」 경고가 다시 등장·알림·횟수 +1 된다** | 드라이버를 쥔 채 손이 잠깐 안 보임(fingertip None) → `update` 가 None → wrong_tool None → 배너 숨김 → 다음 결과에서 다시 오답 → 배너가 다시 미끄러져 나오고 알림 1건, 결과창 「다른 공구 N회」가 +1 된다. 결과창의 횟수가 「집은 횟수」가 아니라 검출 깜빡임 수가 된다 | 설계 §3.3 은 **횟수**를 요구한다. 손 검출이 끊기는 빈도는 실기동에서 확인해야 한다 |
| 13 | 낮음 | 확실 · 재현✓ | safety_console.py:1332-1337, 1131-1134, 509-547 | **스크림(어둡게 하기)을 상태에서 계산하지 않는다.** 메뉴·설정을 연 채 손이 버튼을 들락거리면(MONITOR↔PROCESS_RUN) `_dim_others(False)` 가 스크림을 끈다. BLOCK 중에 메뉴를 열었다 닫아도 배너 뒤 어둡게 하기가 사라진다 | 작업 중 메뉴 열기 → 손 진입·이탈 → 메뉴는 열려 있는데 스크림이 없다(재현) | `_sync_cta_visibility` 는 「기억하지 말고 계산」 원칙을 지키지만 스크림은 그렇지 않다 |
| 14 | 낮음 | 확실 · 재현✓(상태 패널) | overlay.py:177-216 · overlay_menu.py:305-312 · overlay.py:486-488 · GlowFrame.set_level | **테마를 바꿔도 다시 칠하지 않는 곳이 더 있다**(결과창은 §4.4 에 있음). ① 공정 단계 행은 다음 FSM 전이 때까지 옛 색이다. IDLE 에서 바꾸면 작업 시작 전까지다(재현: QSS 그대로). ② 이미 쌓인 알림 행은 다크 글자 `#e6e8ea` 가 화이트 시트(0.92 흰색) 위에 남아 **거의 안 보인다**. ③ 차단 맥박 색은 AlertBanner 생성 때 다크 `#ff5252` 로 굳는다. theme.py 는 화이트에서 더 어두운 `#a80000` 을 요구한다. ④ 발광 색(#7 이 고쳐지면 드러난다) | 설정 → 화이트 → 알림 창 열기 → 옛 알림 글자가 흰 바탕에 묻힌다 | `_apply_theme` 은 패널의 `apply_theme` 만 부르고 `update_view`·`push` 로 칠한 색은 다시 칠하지 않는다. RecordPanel.apply_theme 은 `set_state(recording)` 을 path 없이 불러 녹화 중이면 파일명 줄이 지워진다 |
| 15 | 낮음 | 확실 | overlay.py:311-342, 401-409 | 게이지의 `_shown_progress`·`_target_progress` 가 서브 종료 뒤에도 남아, **다음 서브 게이지가 가득 찬 상태에서 0으로 줄어드는 애니메이션**으로 시작한다(애니메이션 켬, 기본값) | B1 서브 완료(1.0) → B2 누름 → 첫 틱에 1.0→0.0 tween(200ms) → 막대가 번쩍 찼다 비워진다 | `update_view(None)`·`_stop_pulse` 가 진행률을 초기화하지 않는다 |
| 16 | 낮음 | 확실 | safety_console.py:1559-1567, 1386-1419 | **종료 경로가 공구 워커를 내리지 않고(`_end_tool_scan` 없음) 상태 파일도 지우지 않는다.** B2 서브 중 ESC·종료하면 워커는 ppid 검사에만 기댄다(§4.4 tool_worker 의 약점과 겹친다). 상태 파일은 유령으로 남는다 | 실제 흔적: `/dev/shm/sop_state/state.json` 이 2026-09-23 23:28 에 기록된 채 남아 있다(pid 21804, 상태 BLOCK). voice_card 의 pid 생존 검사로 대부분 걸러지지만 pid 가 재사용되면 새지 않는다는 보장이 없다 | `closeEvent` 에 `_state_pub.clear()`·`_end_tool_scan()` 호출이 없다 |
| 17 | 낮음 | 확실(잠재) | safety_console.py:115, 171 · recipe.py:48 · gpio_input.py:73 · safety_console.py:1290 | 레시피를 검사하는 쪽과 쓰는 쪽의 가정이 어긋난다(§4.1-9 는 dwell 만 다룸). `process_name`·`emo_button` 이 없어도 `_validate` 는 통과하는데 GUI 는 `[...]` 로 직접 읽어 **기동 중 KeyError** 가 난다. GPIO·키보드는 `"EMO"` 를 박아 두었다. 레시피의 emo_button 을 다른 이름으로 바꾸면 **물리 EMO 가 「오답 버튼」으로 처리돼** 위반 BLOCK·기대단계 유지가 된다 | 현재 recipe.json 은 두 키가 다 있어 지금은 문제가 없다 | — |
| 18 | 낮음 | 가능성 | safety_console.py:857 | 체류 시간을 **GUI 가 신호를 받은 시각**(`time.time()`)으로 잰다. 프레임 시각이 아니다. GUI 스레드가 멈추면(서브 종료 때 `tool_gate.stop()` 의 `wait(timeout=2)`, 녹화 시작, 로그 파일 줄마다 열기 등) 큐에 쌓인 ROI 신호 사이 간격이 늘거나 줄어 체류 판정이 흔들린다 | B2 서브 종료 → GUI 가 워커 종료를 기다리는 동안 손이 B3 링 → 대기 뒤 연속 두 프레임 간격이 실제보다 길게 계산된다 | 실제로 얼마나 멈추는지는 실측이 필요하다 |
| 19 | 낮음 | 확실 | safety_console.py:536 | 설정 창의 「공구 변경 가능」을 **열 때 한 번만** 계산한다. IDLE 에서 열어 둔 채 #5 경로(EMO→해제→PROCESS_RUN)로 작업이 시작되면 작업 중에도 공구를 바꿀 수 있다 | 설정 열기(IDLE) → GPIO EMO → 해제 → 창을 연 채 공구 변경 → 다음 B2 서브 판정 공구가 바뀐다 | — |
| 20 | 낮음 | 확실 | safety_console.py:1185-1192, 1209-1216, 1374-1381 | 거부창·폴트창을 **누를 때마다 새 비모달 QMessageBox 로** 띄운다. EMO 를 복귀하지 않은 채 「차단 해제」를 여러 번 누르면 창이 겹겹이 쌓여 시연 화면을 가린다 | EMO 누른 채 해제 5번 → 경고창 5개 | 참조를 들고 재사용하지 않는다 |

## 요약
- 높음 3 · 중간 9 · 낮음 8 (합 20)
- 가장 급한 셋은 모두 **서브 작업이 FSM 상태(BLOCK·WARNING·EMO)와 따로 도는 것**(#1)과 **공구 경고가 FSM 배너를 덮는 것**(#2·#3)에서 나온다. 셋 다 헤드리스로 재현했다.
- #7·#8 은 오프스크린 픽셀로 확인했다. 기본 설정에서는 발광 테두리와 배너 판·차단 맥박이 **화면에 그려지지 않는다.**
