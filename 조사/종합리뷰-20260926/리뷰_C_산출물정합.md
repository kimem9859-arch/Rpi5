## 관점 C(산출물 정합) 리뷰 보고: 「실제 시연 프로그램 문제 수정」 ①~④ 산출물

### 요약
- 코드와 가장 자주 대조되는 서술은 Rpi5 HEAD(`cfd52a3`) 코드와 맞습니다. 대상은 §6.3 추적 규칙 ①~⑤, §7.1~7.4(EMO 해제 → IDLE, 방금 버튼·EMO 체류 제외, 체류 시각 = 프레임 수신 단조 시각), §8.1 포트 찾기·사유 로그·「연결됨」 = ACK·늦은 ACK 비우기·C18, §8.2 일시정지/취소·G3·D3·배너 문구, §9.4입니다.
- 수치와 기록 파일도 맞습니다. §12.74·§12.75·[CURRENT]의 72.1%·0.78회/분·127→123은 원출력과 같습니다. 자가 테스트 27/28/31/32개는 모두 exit=0이고, `fsm_sim --gate` 출력은 ③ 뒤·④ 전후 5개 파일이 바이트까지 같습니다. 작업로그 🔗의 커밋 해시는 전부 실재합니다.
- 어긋남은 다섯 갈래입니다.
  - 옛 규칙 「경고 중 정답 = 단계 완료」가 §5.3에 caveat 없이 남았습니다.
  - CH5 실물 우회 조건이 §8.2 배너 서술에 빠졌습니다.
  - 🔓 B3→EMO 행이 ①P3 이후에도 「FSM 반응 미확인」입니다.
  - 저널 §12.74-(2)의 P1 오경보 감소 폭이 표와 다릅니다.
  - 알려진 위험(전역 예외)이 🔓 표에 없습니다.
- 함수목록 §1·§4.4, 계획서 상태 줄, TESTING_FSM에 낡은 곳이 여럿 남았습니다.
- Critical은 없습니다.

### 발견

#### Critical
없음.

#### Important

**I-1. §5.3이 D3를 「단계 완료」로 글자 그대로 적었습니다(D3가 명시적으로 거부한 해석).**
- 위치: `docs/통합문서.md:420` — 「**정답 버튼을 누르면 경고 해제 + 단계 완료**」. 서브 작업 단계 예외 caveat가 없습니다.
- 실제 코드:
  - `Rpi5/Demo/safety_console.py:1017-1041` — 서브가 있는 단계에서 경고 중 정답을 누르면 `release_warning()` 뒤 서브를 재개하거나 새로 시작합니다. 단계 완료는 서브가 끝난 뒤입니다.
  - `recipe.json`은 B1·B2·B3에 서브 10초가 있고, 즉시 완료되는 것은 B4뿐입니다.
  - 설계서 §2.2는 「단계 완료를 글자 그대로 쓰면 남은 10초 대기를 건너뛰는 우회로」라고 적었습니다. §7.3-6(613행)·§8.2(704행)는 올바르게 적었습니다.
- 같은 단순화가 남은 곳:
  - `docs/변경이력.md:89` 「현행은 경고 해제 + 단계 완료(§7.3-6)」
  - 🔓 행(`통합문서.md:1168`) ① 부분 「경고 중 정답 → 해제 + 단계 완료」
  - `Rpi5/CLAUDE.md:13`
  - 이 셋은 판정기 수준 서술로 읽을 수는 있습니다.
- 오해: 사용자 흐름(§5.3)을 제출 문서·시연 설명에 옮기는 사람이 「경고 중 정답을 누르면 단계가 넘어간다」고 적게 됩니다. 이는 실제 동작이 아니고, 거부된 우회 동작입니다.
- 고칠 방향: §5.3을 「경고 해제 + 평소 누른 것과 같게(§7.3-6)」로 바꿉니다. 변경이력 「현행」 칸도 서브 단계 예외를 함께 가리키게 합니다.
- 확인 수준: 대조함.

**I-2. §8.2·§8.1이 「인터락 연결 = 버튼 전기 차단」을 전제로 씁니다. CH5 실물 우회 조건이 빠졌습니다.**
- 위치:
  - `통합문서.md:703` 「인터락이 버튼 전기를 끊지 못하는 경우(**키보드·릴레이 미연결**)에도」
  - `:706` 「인터락이 연결되지 않은 채 차단되면 … 전기는 끊기지 않았으므로 끊겼다고 믿게 두지 않는다」
  - `:661` 표 BLOCK 「전기 | **차단**」
- 실제 문서 기록:
  - 🔓 CH5 행(`:1143`) — 「릴레이 우회로 되돌림. 지금 차단 = 램프·부저 + 소프트웨어 무시(`fsm.py`)뿐」
  - 같은 절 C18 문장(`:665`)은 「지금 실물은 CH5가 릴레이 우회」라고 조건을 달았습니다.
- 실제 코드: `safety_console.py:1196-1209`의 「화면에서만 차단 중」 줄은 `interlock.connected`(ACK)만 봅니다. Arduino가 붙어 ACK하면 이 줄이 사라지지만, 버튼 GND는 끊기지 않습니다.
- 오해: 연결 시 버튼 입력이 물리적으로 차단된다고 믿게 됩니다. 시연 설명이나 평가 문서에 과장이 들어가고, 배너가 「화면에서만」을 안 띄우는 상태를 사실로 오해합니다. 실HW 미검증 과장에 해당합니다.
- 고칠 방향: 703·706·661에 「지금 실물은 CH5 우회. 연결돼도 버튼 전기는 끊기지 않는다(램프·부저만) · §12 🔓 CH5」 한 줄을 붙입니다. 배너 문구를 바꿀지는 사용자 결정 사항입니다.
- 확인 수준: 문서·코드는 대조함. 실물 상태는 🔓 행 인용(추론).

**I-3. 🔓 「B3→EMO 반복 오분류」 행이 ①P3 이후에도 「FSM 반응 미확인」입니다.**
- 위치: `통합문서.md:1157` — 「EMO 는 비상정지 클래스라 무겁다 — B3 를 누르려는 손이 EMO 로 판정될 수 있다(**FSM 반응 미확인**)」
- 실제로는 반응이 규명된 뒤 바뀌었습니다.
  - 검토 C3(`Rpi5/조사/런타임-검토-20260924/review_core.md:14`)가 반응을 규명했습니다(가짜 EMO 체류 → 헛경고). 원문에 「문서가 FSM 반응 미확인으로 남긴 … FSM 쪽 결과가 이것이다」라고 적혀 있습니다.
  - ① P3(`fsm.py:216`)로 EMO는 체류 판정에서 빠졌습니다. 따라서 지금 반응은 「경고 없음」이고, 대가는 오답 B3가 EMO로 잡히면 위반 머묾을 놓치는 것입니다(저널 §12.74-(3) 「EMO 제외의 대가」).
  - 비전 EMO는 BLOCK을 만들지 않습니다(EMO 차단은 GPIO 경로뿐).
- 연관: 저널 `성능검증-저널.md:4633`의 「트래킹 보강(③묶음 R2)이 다룬다」 → R2는 한 프레임짜리 가짜만 걸러냅니다(§12.75). 실콘솔 B3→EMO는 「확정 기준 57~94건」으로 여러 프레임 지속이라, R2로 해소됐다고 읽히면 과장입니다.
- 오해: 라벨링 우선순위를 판단할 때 위험 방향을 거꾸로 봅니다. 「무거운 오경보」로 보게 되는데, 실제로는 「오답 머묾 놓침」입니다.
- 고칠 방향: 행의 괄호를 「①P3 뒤 EMO 체류는 경고하지 않음 — 오답 B3 머묾을 놓치는 쪽(§12.74-(3)) · R2는 한 프레임 가짜만」으로 바꿉니다.
- 확인 수준: 대조함.

**I-4. 저널 §12.74-(2) 요약 문장의 P1 오경보 감소 폭이 같은 절 표·원출력과 다릅니다.**
- 위치: `성능검증-저널.md:4626` — 「「체류 = 실제 관측 구간」·「긴 공백 = 새 체류」는 **각각** 사전 차단 14건을 잃고 **오경보 2건**을 줄였다」
- 실제: 같은 절 표와 `Rpi5/조사/판정수정-20260925/preview_variants.txt`는 다음과 같습니다.
  - P2+P3 = 14건 / 132
  - +P4 = 12건 / 118 → −2건
  - +P1 = **10건** / 118 → **−4건**
- 오해: 보류된 P1을 「측정 도구 정합·값 재설정 때 다시 본다」(§7 ⏸)에서 재검토할 때, P1의 헛경고 감소 효과를 절반으로 과소평가합니다(남은 14건 중 4건, 약 29%).
- 고칠 방향: 「각각 사전 차단 14건을 잃고 오경보를 2건(긴 공백)·4건(실제 관측 구간) 줄였다」로 바꿉니다.
- 확인 수준: 대조함.

**I-5. 알려진 위험 「슬롯 예외 하나로 시연 프로그램 전체가 끝남(전역 예외 처리 결정 대기)」이 정본 🔓 표에 없습니다.**
- 위치: `docs/작업로그.md:23`에만 ⏸로 있습니다. 통합문서, 설계서 §9, 계획서 ④-B, 함수목록 어디에도 없습니다(grep 0건).
- 실제: 런타임에 `excepthook`이 없습니다(`Rpi5/Demo/*.py` grep 0건). ④ 최종 리뷰 Critical(U15 회귀, `dc4efb2` 「슬롯 안에서 앱을 끝내던 회귀」)이 이 위험이 실제로 일어난 사례입니다.
- 규칙: `.claude/rules/문서배치.md`는 「미결·알려진 위험 = §12 머리 🔓 미결 표」라고 정합니다.
- 오해: 정본만 읽고 시연을 준비하는 사람이 「어떤 GUI 예외든 앱이 끝난다」는 위험을 모릅니다. 결정 대기 사실 자체는 이미 알려진 것이므로, 여기서는 배치 누락만 보고합니다.
- 고칠 방향: 🔓 행을 추가하거나, 「실제 시연 프로그램 문제 수정」 행의 ⏸ 남은 것에 한 줄 넣습니다.
- 확인 수준: 대조함.

#### Minor

1. **🔓 「실제 시연 프로그램 문제 수정」 행**(`통합문서.md:1168`):
   - 「결정 D1~D9」인데 설계서에는 D10이 있습니다.
   - 「「연결됨」 = 마지막 명령 ACK(**§8.2**)」인데 실제 서술은 §8.1 `:670`에 있습니다.
   - 「녹음 파일 닫기(**§9.4**)」인데 §9.4에 녹음 파일 서술이 없습니다.
   - ④ 부분에만 ①②③ 형식의 「✅ … 완료(날짜 · 공동 확인)」 표지가 없습니다(제목에만 「완료」).
   - 확인 수준: 대조함.
2. **§8.2 EMO 배너 인용**(`:705`): 「비상정지로 차단됐습니다 — EMO 를 복귀한 뒤 해제하세요」로 적혀 있지만, 실제 둘째 줄은 「— EMO 를 복귀한 뒤 「차단 해제」를 누르세요」입니다(`safety_console.py:1206-1208`). 「비상정지로 작업 중단」 알림은 작업이 진행 중일 때만 뜹니다(`was_running`, `:1323`). 확인 수준: 대조함.
3. **§8.1 인터락 서술에 실HW 미확인 표시가 장 안에 없습니다**(`:669-673`). 「켠 뒤에 꽂거나 번호가 바뀌어도 스스로 붙는다」·늦은 ACK 비우기·지정 포트는 가짜 시리얼로만 확인됐습니다(설계서 §9.4). §9.4는 「⏸ 실재생 확인 전」을 인라인으로 달았지만 §8.1은 🔓 행에만 의존합니다. 확인 수준: 대조함.
4. **④ 동작 사실이 🔓 행에만 있습니다(R12).**
   - 대상: 레시피에 `process_name`이 없거나 `emo_button`이 config와 다르면 레시피 전체를 거부하고 **기본 시퀀스(B1~B4, 서브 작업·공구 게이트 없음)**로 떨어지는 것(`recipe.py:52-63`, `safety_console.py:101-106`). 스크림 계산, 알림 창 하나, 프레임 처리 오류도 마찬가지입니다.
   - 🔓 행은 닫히면 지우는 자리라 이 사실들이 사라집니다.
   - §5.1.1 「레시피가 이긴다」(`:382`)에는 거부 시 대체 동작이 없습니다.
   - 확인 수준: 대조함.
5. **§15 역사 기록**(`:1405-1406`): 「①Arduino USB 분리 중 위반 → 무ACK 폴트 경고창」은 ③ I1·I2 뒤 대개 경고창 대신 배너로 바뀌었습니다. 재연결 루프가 먼저 끊김을 보면 미연결 보류가 됩니다(`interlock.py:166-170, 270-275`). 주석이 없습니다. 확인 수준: 추론(타이밍 의존).
6. **측정 도구 정합 항목이 문서마다 다릅니다.**
   - 작업로그 `:25`는 측정 도구 정합에 「④ 체류 시각 출처」를 넣었습니다.
   - 🔓 「측정 도구와 실제 시연 프로그램의 불일치」 행(`:1167`)에는 R2만 있고 C16은 없습니다.
   - §7.4 `:635`는 「fsm_sim은 무관」이라고 적었습니다.
   - 또 `Rpi5/도구목록.md:24`의 bench_detector 「실제 시연과 다른 점」에 R2(두 프레임 확정·오래된 트랙 우선)가 반영되지 않았습니다. ③ 계획서 22행은 「Task 13에 기록」이라고 했습니다.
   - 확인 수준: 대조함.
7. **[CURRENT] 위반 사전 차단율**(`:981`): 새로 붙은 「규칙 변경 후 72.1%」에 시뮬레이션 정책 4종 병기가 없습니다. E2E 행(`:982`)은 「병기 없이 인용 금지」를 답니다. 종전 형식을 따른 것입니다. 확인 수준: 대조함.
8. **설계서 본문 낡음**(상태 줄은 맞음):
   - §3.2 G8(73행) 「설계대로 그린다 · 붉은 테두리」인데, 실제는 가장자리 테두리를 쓰지 않습니다(`theme.GLOW_BORDER = False`).
   - §5 V1 행(116행)에 ⏸가 없습니다. I1~I3 행에는 있습니다.
   - I2(96행) 「by-id·VID/PID로 다시 찾는다」인데, 실제는 「ESP32 아닌 USB 시리얼이 정확히 하나」입니다(`config.py:315-339`). 정본은 8686b7e에서 정정됐습니다.
   - R2(86행) 「자리 잡은 트랙은 새 트랙에 밀려나지 않는다」는 실제(진짜가 지금 보일 때만)보다 강합니다. §6.3에 한계가 적혀 있습니다.
   - 09-25에 쓴 문서인데 재편 전 번호 「§10.61」(103행), 「§10.32-(6)③」(39행)을 씁니다. 현행 번호는 §12.61·§12.32-(6)③입니다.
9. **계획서 상태 줄**:
   - ④-A(3행) 「다음 = ④-B」가 두 번 나옵니다. ④-B는 이미 완료돼 낡았습니다.
   - 커밋 범위 `2673e6a..672603d`(④-A)·`5365156..32a4304`(④-B)가 문서 커밋 `5365156`(④-A 반영)·`232157c`(④-B 반영)를 빠뜨립니다.
   - ③ 「Task 0~12」와 작업로그 「Task 0~13」은 사소한 차이입니다.
10. **함수목록**(`Rpi5/함수목록.md`):
    - §1 흐름도 `:20`에 「⚠ 박스가 그려진 프레임을 받는다(4절)」가 남았습니다. R3로 해결됐고 §4.1-1도 해결로 표시돼 있습니다.
    - §4.4에 해결 표시가 없는 메모 7행:
      - `:722` `_process_frame`, `:745` `_on_boxes_changed`(R3)
      - `:746` `_publish_state`(G7)
      - `:763` `SettingsPanel`(G6)
      - `:783` `_feed`, `:784` `FfmpegSet.start`(G12)
      - `:791` `is_wake`(V3)
    - `:785` `AudioLog.close`는 해결 표시를 달고도 M-3가 틀렸다고 밝힌 「헤더가 마무리 안 될 수 있음」을 유지합니다.
    - 머리 `:4`:
      - 「8,998줄」은 ④ 뒤 34개 모듈 실측 9,228줄입니다.
      - 「새 함수 20개 · ④ 13개」는 394에 더하면 427이지만 실제는 426입니다. 한 개가 없어진 것이 설명되지 않았습니다. 426과 모듈별 개수는 AST로 대조한 결과 맞습니다.
    - 갱신되지 않은 행:
      - `_validate`(`:271`, U17 규칙 없음)
      - `closeEvent`(`:399`, U16 없음)
      - `_show_block_banner`(`:402`, 부르는 곳에 `_update_conn_bar` 누락)
      - `_drop`(`:315`, `_reconnect_loop` 누락)
      - `_update_tracks`(`:96`, `YOLO_CONFIRM_HITS` 누락)
    - 확인 수준: 대조함.
11. **`Rpi5/CLAUDE.md`**:
    - `:13` 「WARNING 중 정답 = 해제 + 단계 완료」에 GUI 서브 단계 예외가 없습니다.
    - 데이터 흐름의 `update_vision(roi, now, level)`에 C16(프레임 수신 시각)이 반영되지 않았습니다.
    - `:5` 「통합수행설계문서…」 정본 파일명이 낡았습니다(이번 작업 전부터).
12. **`Rpi5/Demo/docs/TESTING_FSM.md`**:
    - ①에서 EMO 줄만 고쳤습니다.
    - D3(경고 중 정답)가 시나리오에 없습니다.
    - 테스트 수 「14/14·5/5·2/2」(`:54-57`)는 실제 39·15·3입니다.
    - 「1초 이상 체류」(`:88`)인데 운용값은 0.3초입니다.
    - USB 웹캠 CCTV(`:94`)는 제거됐습니다.
    - 대부분 이번 작업 전부터 낡았던 것입니다.
13. **`dev/fsm/README.md:9`**: 「EMO → 해제 시 기대단계=1 리셋」에서 IDLE이 빠졌습니다. 틀리지는 않았지만 불완전하고, 「정본 §9」는 옛 번호입니다.
14. **§8.2 `:707`**: 「손이 빈손으로 보이면 내려놓은 것으로」인데, 실제는 빈손 스캔 3번 연속입니다(`config.TOOL_PUT_DOWN_SCANS=3`, `safety_console.py:918-924`).

### 점검 표

| 문서 | 대조 항목 | 맞음 | 어긋남 |
|---|---|---|---|
| 설계서 `2026-09-25-런타임-문제수정-design.md` | 30 (상태 줄·D1~D10·P/G/R/I/V/C/U/M 행 ↔ 코드·§9 판단 C18·U19) | 25 | 5 (M-8) |
| 계획서 5개(머리·관문·Expected 개수·커밋 범위) | 20 | 17 | 3 (M-9) |
| 통합문서 §5.1~5.3·§6.3·§7·§8.1·§8.2·§9.4·§12 [CURRENT]·🔓 3행·§15 | 45 | 35 | 10 (I-1·I-2·I-3·I-5·M-1~M-5·M-7·M-14) |
| 저널 §12.74·§12.75(수치 12개·마커·조건·해시 `bc95c75`) | 16 | 14 | 2 (I-4 · I-3 연관 `:4633`) |
| 변경이력(이번 폐기 5행 이관) | 6 | 5 | 1 (I-1 연관 `:89`) |
| 함수목록(머리·모듈별 개수·새 행·§1·§4.1·§4.4) | 30 | 16 | 14 (M-10) |
| Rpi5/CLAUDE.md(FSM·카메라·추적) | 6 | 4 | 2 (M-11) |
| TESTING_FSM.md | 6 | 1 | 5 (M-12) |
| 조사/README·기록 파일(selftest 9개·gate 9개·probe·preview·metrics) | 24 | 24 | 0 |
| 작업로그 09-24~26(사실·수치·🔗 해시 약 90개) | 14 | 14 | 0 |

### Declined to judge
- `docs/변경이력.md:132`의 `7e28f90`: 두 저장소 모두에 없습니다. 이번 작업과 무관한 hanium-docs 규칙 행이고, project-docs 저장소 해시일 수 있습니다.
- 🔓 행 「멎은 스트림은 더 빨리 다시 붙음」(C19): 코드 경로(`_close_sock` shutdown → join 즉시 반환)상 타당하지만, 얼마나 빨라지는지는 실행 없이 판정하지 않았습니다.
- `fsm_sim`이 쓰는 `hoi.db` 시각과 런타임 「카메라 수신 단조 시각」이 도구·런타임 차이인지: DB 시각의 출처를 확인하지 않았습니다.
- 「영상 폭의 46%」: `parent_rect`가 영상 영역과 같은지. 화면 모습·G8 사진·1280×720 외관은 시각 확인이 필요합니다.
- ③ 리뷰 M# 원문(세션 `ac3b92f1` 보존본)은 저장소 밖이라 설계서 §9.1 매핑을 원문과 대조하지 못했습니다.
- 실HW 동작 전반(Arduino·서브 ESP32·실기동 화면)은 이미 알려진 ⏸입니다.

참고: 읽기 전용으로만 진행했고 git 상태는 바꾸지 않았습니다. 자가 테스트·Hailo import는 실행하지 않고 기록 파일만 읽었습니다. 하위 에이전트는 쓰지 않았습니다. 통합문서·저널은 절 단위로, 계획서는 머리·관문 위주로 나눠 읽었습니다(계획서 Task 본문의 Expected 전문 대조는 하지 않음).
