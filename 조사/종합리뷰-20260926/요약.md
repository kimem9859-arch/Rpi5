# 실제 시연 프로그램 문제 수정(①~④) 종합 리뷰 — 통합 목록

> 2026-09-26 · 세션 `4c6a495c`(← `baa57676` 이어짐) · 사용자 「실제 시연 프로그램 문제 수정 결과, 산출물에 대해서 재검토, 리뷰를 종합적으로 한번 진행하고 싶어」 → 계획 승인 「진행」
> 범위 = Rpi5 `1871139..cfd52a3`(70커밋) + 설계서·계획서 5개·통합문서 해당 절·저널 §12.74·§12.75·함수목록·Rpi5 CLAUDE.md·조사 기록 · **읽기 전용 · 코드 수정 없음**
> 방법 = 새 맥락 리뷰어 3명 동시(opus) — A 묶음 사이 맞물림(코드 추적 + 격리 사본 재현) · B 실제 조건 실행(기본 설정·이벤트 루프·가짜 ESP32/검출기/시리얼·시나리오 30여 개 · 별도 프로세스) · C 산출물 정합(문서 ↔ 코드·기록 대조)
> 원문 = 같은 폴더 `리뷰_A_교차맞물림.md` · `리뷰_B_실제조건실행.md` · `리뷰_C_산출물정합.md` · 번호(A-I1 · B-M3 · C-M10 …)는 원문 번호다
> B 하네스 = `~/lab/runtime-review-harness/`(git 밖 · 이 파이에만 · 시나리오 28개 · 실행법 = 원문 B 끝) — 🔴 저장소에 넣기 전에 읽고 판단할 것(검토하지 않은 코드)

## 결론

- **Critical 0 · 이번 네 묶음이 만든 회귀 0.** 지금 코드(HEAD)에서 실제 조건 시나리오 30여 개 모두 abort·예외 없음. ④ 최종 리뷰 Critical(게이지 보간 → abort)은 수정 전 사본 `dc4efb2~1` 에서 exit 134 로 재현되고 HEAD 에서 안 남 = 수정 확인.
- 규칙 변경 D3·D4·D5 교차 · 모든 상태의 EMO · 배너의 인터락 연결 추종 · 8바퀴 소크(메모리·스레드 증가 없음) · 정본 수치·기록 파일·작업로그 해시는 맞다.
- **중요 7건(아래) · 사소 코드 12 · 사소 문서 14.**

## 중요 7건 — 다음 세션에서 한 묶음으로 고친다(추천 순서)

| # | 내용 | 위치 | 출처 · 확인 |
|---|---|---|---|
| 1 | 🔴 **작업 초기화 확인창(모달)이 떠 있는 동안 EMO 가 들어오면 「예」 한 번으로 EMO 를 누른 채 인터락이 풀린다**(IDLE · 릴레이 RUN · 「작업 시작」 가능). EMO 미복귀 검사를 확인창 **앞에서만** 하고, 모달 중첩 루프가 GPIO 큐 신호를 처리하며, `_reset_work()` 는 다시 보지 않는다 | `safety_console.py` `_on_reset_clicked`(EMO 검사 → `QMessageBox.question` → `_reset_work()`) · `fsm.py` `reset()` | A-I1 · B-I1 **둘 다 재현** · 코드 확인(세션) · **이번 작업 이전부터**(5b60a90) · 고치는 방향 = 확인창이 돌아온 뒤 `gpio_input.emo_active()`(또는 `fsm.emo_active`) 다시 보고 거부 |
| 2 | **U15(다음 서브 게이지 번쩍임) 수정이 기본 설정(애니메이션 켬)에서 반쯤만 듣는다** — `update_view` 가 진행률이 바뀔 때마다 새 보간을 만들고 `_gauge_anim` 을 덮어써 앞 보간을 멈추지 않는다(`_sub_timer` 200ms = `D_GAUGE` 200ms → 최대 2개 겹침). 숨길 때 마지막 것만 멈춰 앞 보간이 `_shown_progress` 를 약 1.0 으로 되돌린다. ⚠️ **④ 에서 U15 를 「완료」로 기록한 것이 기본 설정에서는 사실이 아니다**(시험이 애니메이션 끈 조건만 봄) | `overlay.py` `GaugePanel.update_view`(숨김 분기 · 보간 생성) | B-I2 재현(10초·2초 모두 0.986~0.998 남음 · 애니메이션 끄면 0) · 코드 확인(세션) · 고치는 방향 = 새 보간 전에 앞 보간 멈춤(`sip.isdeleted` 가드 유지) 또는 세대 번호 · 🔴 시험은 애니메이션 켠 채 이벤트 루프로(하네스 `u15_gauge`) |
| 3 | 정본 §5.3 「정답 버튼을 누르면 경고 해제 + **단계 완료**」 — D3 가 거부한 글자 그대로의 해석. 실제는 서브 있는 단계(B1~B3)면 서브를 잇거나 시작(§7.3-6·§8.2 는 맞음) | `docs/통합문서.md` §5.3(「이상」 줄) · 같은 단순화 = `docs/변경이력.md`(현행 칸) · 🔓 「실제 시연 프로그램 문제 수정」 행 ① 부분 · `Rpi5/CLAUDE.md` FSM 줄 | C-I1 · 대조함(세션 확인) |
| 4 | **차단 표현이 실제보다 강하다** — 정본 §8.1 표(BLOCK 「전기 차단」)·§8.2(「인터락이 연결되지 않은 채 차단되면…」 = 연결되면 전기가 끊긴다는 전제)와 화면 첫 줄(「순서 위반이 계속되어 인터락이 작동했습니다」)·알림(「전기 입력 차단됨」)·결과창(「인터락 작동 N건」)·음성 카드(「전기 입력이 끊겼다」). 지금 실물은 CH5 우회라 연결돼도 버튼 전기는 끊기지 않는다(램프·부저만 · 🔓 CH5 행) | 통합문서 §8.1·§8.2 · `overlay.py` 차단 기본 문구 · `safety_console.py` 알림 · `overlay_result.py` · `voice_card.py` | C-I2(문서) · A-M4(화면·음성 — 배너 재현) · 🔑 **화면 문구를 바꿀지는 사용자 결정**(문서 조건 추가는 바로) |
| 5 | 🔓 「B3→EMO 반복 오분류」 행이 아직 「FSM 반응 미확인」 — 검토 C3 가 규명했고 ① P3 뒤 EMO 체류는 경고하지 않으므로 위험은 「오답 B3 머묾을 놓침」(저널 §12.74-(3)). 저널 한 줄의 「R2 가 다룬다」도 과장(R2 는 한 프레임 가짜만) | 통합문서 🔓 표 B3→EMO 행 · `docs/성능검증-저널.md` 해당 줄 | C-I3 · 대조함 |
| 6 | 저널 §12.74-(2) 요약 문장 「「체류 = 실제 관측 구간」·「긴 공백 = 새 체류」는 각각 … **오경보 2건**을 줄였다」 — 원자료는 P4 −2건 · **P1 −4건**(P2+P3 14 → +P4 12 · +P1 10) | `docs/성능검증-저널.md` §12.74-(2) · 원자료 `Rpi5/조사/판정수정-20260925/preview_variants.txt` | C-I4 · 원출력 대조(세션 확인) · 🔴 시뮬레이터 수치 → 정책 4종 병기(`rules/수치인용.md`) |
| 7 | 알려진 위험 「화면 처리(슬롯) 예외 하나로 시연 프로그램 전체가 꺼진다 · 전역 예외 처리 결정 대기」가 작업로그 ⏸ 에만 있고 정본 🔓 표에 없다(`rules/문서배치.md` = 미결·위험은 🔓) | 통합문서 §12 🔓 표 | C-I5 · 대조함 · 전역 처리 자체는 **사용자 결정 대기** |

## 사소 — 규칙대로 목록만(고칠지는 사용자가 정한다)

**코드 12**
- A-M1 카메라 재연결 × 방금 누른 버튼(P2) × 두 프레임 확정(R2) — 재연결 첫 None 프레임에 방금 완료 예외가 지워져, 손가락을 그 버튼에 둔 채 재연결되면 드문 헛경고(R5 의 「예외 유지」 약속이 실제 경로에서 안 지켜짐 · 재현)
- A-M2 작업 초기화 뒤 EMO → 해제하면 거짓 「비상정지로 작업 중단」 알림(`_reset_work` 가 집계 `running` 을 안 비움 · 재현)
- A-M3 작업 전·완료 뒤 EMO 에 음성 상태 파일이 「진행 중 · 1단계 · 비상정지」 · 완료 결과가 사라짐(재현)
- A-M5 오답 공구를 쥔 채 차단 → 재시도하면 「다른 공구」 2회 집계(G11 × D5 · 재현)
- A-M6 레시피 검사가 서브 사양을 다 덮지 않음 — `tool_names: null` → 공구 단계 시작 때 앱 종료 · `wait` 서브의 `tool` 키 → 가짜 공구 줄 · GPIO·키보드가 `"EMO"` 를 글자로 박음(config 값과 따로)
- A-M7 서브 타이머·집계가 벽시계(`time.time`) — NTP 로 시각이 튀면 10초 대기가 즉시 끝나거나 오래 멈춤(추론)
- A-M8 해제에 성공해도 「해제 거부」·인터락 폴트 창이 남아 옛 말을 함
- B-M1 단계가 바뀐 직후(막대 300ms·반짝 520ms) 테마 전환 → 공정 단계 행이 옛 색(재현 · U14 ① 의 경합)
- B-M2 작업 전 EMO 차단 중 「▶ 작업 시작」 버튼이 보이고 배너 뒤로 비침(BLOCK 분기가 `_sync_cta_visibility` 안 부름 · 사진)
- B-M3 차단 중 설정 창을 열면 배너가 「화면 테마」 라디오를 덮음 — 겹침 순서가 `_dim_others`·`_relayout` 두 곳에서 따로 정해짐(사진)
- B-M4 배너 등장 중(260ms) 모드가 바뀌면 `_needs_entrance` 가 남아 다음 재배치 때 다시 미끄러져 들어옴
- B-M5 로그 소음 — D3 때 「이어서」 두 번 · 기동 첫 연결에도 R5 줄 · 연결을 받자마자 끊는 상대에 3초마다 끝없는 재연결 로그

**문서 14**(원문 C 의 Minor 1~14)
- 🔓 「실제 시연 프로그램 문제 수정」 행: 「결정 D1~D9」(→ D10) · 「연결됨 = ACK(§8.2)」(→ §8.1) · 「녹음 파일 닫기(§9.4)」(§9.4 에 없음) · ④ 에 「✅ 완료(날짜·공동 확인)」 표지 없음
- §8.2 EMO 배너 인용 글자가 실제와 다름 · 「비상정지로 작업 중단」은 진행 중일 때만 · 「빈손이면 내려놓음」 → 실제 빈손 스캔 3번 연속
- §8.1 인터락 서술에 실HW 미확인(⏸) 표시 없음
- ④ 동작 사실이 🔓 행에만 있음(레시피 거부 → 서브 없는 기본 시퀀스로 떨어짐 등 · §5.1.1 에 없음)
- §15 역사 기록 「USB 분리 중 위반 → 폴트 경고창」이 ③ 뒤 대개 배너로 바뀜(주석 없음)
- 측정 도구 정합 항목이 문서마다 다름(C16 · `Rpi5/도구목록.md` bench_detector 칸에 R2 미반영)
- [CURRENT] 「규칙 변경 후 72.1%」에 시뮬레이션 정책 4종 병기 없음
- 설계서 본문 낡음 — §3.2 G8「붉은 테두리」 · §5 V1 ⏸ 없음 · I2「by-id」 · R2「밀려나지 않는다」(보일 때만) · 옛 절 번호 §10.61·§10.32
- 계획서 상태 줄 — ④-A 「다음 = ④-B」 낡음 · 커밋 범위가 문서 반영 커밋 빠뜨림
- 함수목록 — §1 흐름도 「박스 그린 프레임」 경고 · §4.4 해결 표시 없는 메모 7행 · `AudioLog.close` 옛 이유 · 머리 줄 수(9,228)·함수 수 설명 · 갱신 안 된 행 5개(`_validate`·`closeEvent`·`_show_block_banner`·`_drop`·`_update_tracks`)
- `Rpi5/CLAUDE.md` — FSM 줄 D3 · 데이터 흐름 `update_vision(roi, now…)` C16 미반영 · 「통합수행설계문서」 옛 이름
- `Rpi5/Demo/docs/TESTING_FSM.md` — D3 시나리오 없음 · 시험 수 · 「1초 체류」 · USB 웹캠(대부분 이번 작업 전부터)
- `dev/fsm/README.md` — EMO 해제에 IDLE 빠짐 · 옛 절 번호
- (C Declined) `docs/변경이력.md` 의 해시 `7e28f90` 은 두 저장소에 없음(project-docs 쪽일 수 있음)

## 시험의 빈틈(B)

자가 테스트 대부분이 **이벤트 루프를 돌리지 않거나 애니메이션을 끈 채** 함수를 직접 부른다 — 중요 1·2 가 그 빈틈에서 나왔다. 실제 조건을 한 번도 밟지 않는 항목 목록 = 원문 B 「실제 조건을 밟지 않는 수정 항목 목록」. 하네스로도 못 밟은 것 = R1·R3(실제 Hailo 손) · V1~V4·C20·M-2(음성 데몬·ESP32) · C15 늦은 ACK 타이밍 · U20 창 관리자.

## 다음 세션이 할 일

1. 중요 7건을 한 묶음으로 — 코드 2건(1·2)은 **실패하는 시험 먼저**(1 = 모달 중 EMO 를 흉내 · 2 = 애니메이션 켠 이벤트 루프), 문서 5건은 정정. 4번의 **화면 문구 변경 여부는 사용자에게 묻는다.** 7번의 전역 예외 처리 **도입 여부도 사용자 결정**(🔓 에 올리는 것은 바로).
2. 사소 26건은 목록으로 둔다(사용자가 고르면 처리).
3. 하네스를 회귀 검사로 쓸지 — 저장소에 넣으려면 먼저 읽고 판단한다.
