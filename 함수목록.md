# 함수 목록 — 실제 시연 프로그램(런타임)의 모든 함수

> `run_demo.sh` → `main.py` 로 뜨는 **실제 시연 프로그램**과, 따로 뜨는 음성비서(`run_voice.sh`)의 함수를 전부 모은 파악 문서다(2026-09-24 작성).
> 대상 = `Rpi5/Demo/` 최상위 모듈 **34개 · 클래스 41 · 함수·메서드 428 · 8,998줄**(2026-09-25 ②·③ 수정 뒤 새 함수 20개 · 2026-09-26 ④ 13개 · 종합 리뷰 중요 수정 2개는 손으로 더했고 기존 행의 줄 번호와 줄 수는 작성 시점 그대로다). 측정·촬영 도구는 [`도구목록.md`](도구목록.md) 에 있다.
>
> - **채운 방법** — 함수 이름·위치·부르는 곳·쓰는 설정값은 **코드를 분석해 기계로** 뽑았다(빠짐 없음). 한 줄 설명은 함수 코드를 읽고 썼다. 파일과 줄은 작성 시점 기준이라 코드가 바뀌면 어긋난다.
> - **부르는 곳** — `직접` = 코드에서 이름으로 부름 · `연결` = Qt 신호·콜백으로 이어짐 · `이름 후보` = 같은 이름의 메서드가 여러 클래스에 있어 어느 쪽인지 코드만으로 확정하지 못함 · `Qt` = Qt 가 화면 이벤트로 부름 · `없음` = 이 프로그램 안에서는 아무도 부르지 않음(도구·테스트가 쓰거나 남은 코드).
> - 이 문서는 **코드를 고치지 않았다.** 파악하며 보인 것은 끝의 4절에 목록으로만 남긴다.

## 1. 전체 흐름 — 한 프레임이 판정이 되기까지

```
[ESP32 카메라] ─TCP:8888─▶ camera_thread.CameraThread
   _recv_worker          최신 JPEG 만 받아 둔다(나머지는 버림)
   run                   JPEG 풀기 → _process_frame
   _process_frame        frame_orient.flip → (왜곡보정 remap) → frame_orient.rotate
                         → detector.HailoDetector.detect (640×640 늘려서 Hailo 추론)
                         → _update_tracks (+ _one_per_class · 가려도 5프레임 유지)
                         → _draw_yolo (화면 표시 켜져 있으면 박스를 그림)
                         → hand_tracker.HandTracker.detect (검지 끝 좌표)  ⚠ 박스가 그려진 프레임을 받는다(4절)
                         → zone_at_point → roi_zones.zone_at_point (손끝이 든 버튼·구역 단계)
                         → 신호: change_pixmap_signal · roi_signal · hand_signal · tool_signal …
                                    │
                                    ▼
safety_console.SafetyConsole (GUI, 메인 스레드)
   _on_roi               fsm.SafetyFSM.update_vision(roi, 프레임을 받은 시각, 단계)   ← 순서 판정의 입력
   _press_button  ◀── gpio_input.GpioInputController (버튼 B1~B4·EMO)
                         fsm.SafetyFSM.press_button
   _tick_sub (200ms)     sub_task.SubTask(대기·공구 서브 작업) · tool_state.ToolState
   _on_tool      ◀── tool_gate.ToolGate ◀─ /dev/shm ─▶ tool_worker(별도 프로세스, rfenv · ultralytics)
                                    │
             fsm.SafetyFSM 콜백 ────┼─────────────────────────────────────────┐
   _on_fsm_state (화면·결과창)  _on_interlock → interlock.set_interlock      _on_feedback → interlock.set_feedback
                                    │                 (Serial → Arduino 릴레이 · 타워램프)
                                    ▼
   overlay · overlay_menu · overlay_result (글라스 UI)   session_stats (결과창 재료)
   state_publisher ─ /dev/shm ─▶ voice_assistant (별도 프로세스 · ~/env/tts)
   demo_recorder · demo_ffmpeg (시연영상 촬영 때만)
```

## 2. 모듈 역할

| 묶음 | 모듈 | 하는 일 |
|---|---|---|
| **시작** | `main` | Qt 앱을 만들고 `SafetyConsole` 창을 띄운다 |
| | `config` | 모든 설정값의 정본(임계·경로·포트·색·녹화 등) |
| **입력** | `camera_thread` | ESP32 영상 수신 → 방향 처리 → 버튼 검출 → 추적 → 손 → 구역 판정을 한 스레드에서 돈다 |
| | `gpio_input` | 실물 버튼 B1~B4·EMO 를 Pi GPIO 로 읽어 GUI 에 알린다 |
| | `serial_ports` | 시리얼 장치를 USB 신원으로 찾는다(포트 번호를 믿지 않기 위해) |
| **인식** | `detector` | 버튼 검출 모델(Hailo `.hef`) 추론 — 640×640 으로 늘려 넣는다 |
| | `hailo_device` | 여러 모델이 Hailo 장치 하나를 함께 쓰게 한다 |
| | `hand_tracker` | 손 검출(팜 + 랜드마크 21점) → 검지 끝 좌표 |
| | `frame_orient` | 반전·왜곡보정·회전과 해상도별 보정 파일·링 크기의 단일 출처 |
| | `roi_zones` | 손끝이 어느 버튼의 어느 구역(박스 안·링)에 있는가 — 판정 규칙의 단일 출처 |
| | `tool_gate` · `tool_worker` · `tool_state` | 공구 검출을 별도 프로세스로 돌리고(워커), GUI 에서 켜고 끄며(게이트), 쥐었는지 판정한다(상태) |
| **판정** | `fsm` | 순서 위반 감지 상태기계(IDLE·READY·PROCESS_RUN·MONITOR·WARNING·BLOCK) |
| | `recipe` | 정답 순서(`recipe.json`)를 읽는다 |
| | `sub_task` | 버튼 사이의 서브 작업(대기·공구)을 관리한다 |
| **출력** | `interlock` | FSM 판정을 Arduino 릴레이·타워램프로 보낸다 |
| | `safety_console` | 메인 GUI — 모든 부품을 연결하고 화면·로그·점검·녹화를 맡는다 |
| | `overlay` · `overlay_menu` · `overlay_result` | 영상 위 글라스 UI(상태·게이지·경고·연결 막대) · 메뉴·알림·설정·점검·녹화 창 · 완료 결과창 |
| | `anim` · `theme` | 화면 애니메이션 · 다크/화이트 색 토큰 |
| **점검·기록** | `precheck` | 기동·작업 시작·수동 점검 규칙 |
| | `session_stats` | 한 번의 작업에서 일어난 일을 모은다(결과창 재료) |
| | `fps` | 프레임 도착 간격에서 FPS 를 낸다(중앙값) |
| | `state_publisher` | GUI 상태를 `/dev/shm` 파일로 내보낸다(음성비서가 읽음) |
| **촬영** | `demo_recorder` · `demo_ffmpeg` | 시연영상 촬영 때 영상 3개를 동시에 남긴다 |
| **음성(별도 프로세스)** | `voice_assistant` | 음성비서 데몬 — 호출어 → 띠링 → 질문 → 공구 안내 |
| | `voice_lib` · `voice_card` · `voice_llm` · `voice_tts` | 판정 로직(순수 함수) · LLM 에게 줄 사실 카드 · ollama 호출 · 문장 음성 합성 |

## 3. 함수 목록 (모듈별)


### `main.py` (1개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `main` ⚠️ | 11 | 앱 전체 글꼴·색을 정하고 콘솔 창을 띄워 프로그램을 실행한다. | 직접 `main(최상위)` | `ACCENT` `BG_PANEL` `BG_PRIMARY` `BORDER_COLOR` 외 4 |

### `config.py` (5개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `font` | 30 | 화면 글자 역할에 맞는 글꼴을 만든다. | 직접 `font_report` · `overlay.AlertBanner.__init__` · `overlay.ConnBar.__init__` 외 16 | — |
| `font_report` | 48 | 원하는 글꼴이 실제로 적용됐는지 한 줄로 알려준다. | 직접 `safety_console.SafetyConsole.__init__` | — |
| `demo_wants` ⚠️ | 251 | 이번 촬영 회차가 1인칭·GUI 영상을 찍는지 알려준다. | 직접 `demo_ffmpeg.FfmpegSet.start` · `demo_recorder.DemoRecorder.start` | — |
| `resolve_interlock_port` | 342 | 기동 때 한 번 — 인터록 포트를 찾아 돌려주고 안내를 찍는다(연결할 때마다 부르는 것은 `find_interlock_port`). (③ I1·I2 · ④ M-6) | 직접 `config(최상위)` | — |
| `find_interlock_port` | 315 | 인터록 포트와 못 찾은 사유(없음·후보 여럿·해석 실패)를 함께 돌려준다 — 연결할 때마다 다시 부른다. (④ M-6) | 직접 `resolve_interlock_port` · `interlock.InterlockController._open` | — |

### `camera_thread.py` (33개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `close_detector` | 56 | 프로그램 종료 때 버튼 검출기의 자원을 정리한다. | 직접 `safety_console.SafetyConsole.closeEvent` | — |
| `_iou` | 64 | 두 사각형 박스가 얼마나 겹치는지 비율로 계산한다. | 직접 `_update_tracks` | — |
| `_update_tracks` | 76 | 새 검출 결과로 버튼 박스 추적 목록을 갱신한다. | 직접 `CameraThread._process_frame` | `YOLO_CONF_HIGH` `YOLO_IOU_MATCH` `YOLO_MAX_MISS` |
| `_one_per_class` | 109 | 버튼 종류마다 추적 박스를 하나만 남긴다. | 직접 `_update_tracks` | — |
| `_labeled_boxes` | 132 | 추적 박스를 (버튼 이름, 좌표) 목록으로 바꾼다. | 직접 `roi_at_point` · `zone_at_point` | — |
| `zone_at_point` ⚠️ | 143 | 손끝이 어느 버튼의 박스 안·테두리 링에 있는지 알려준다. | 직접 `CameraThread._process_frame` | — |
| `roi_at_point` ⚠️ | 153 | 손끝이 들어 있는 버튼 박스 이름을 알려준다(링 제외). | **없음** | — |
| `box_bgr` | 161 | 버튼·공구 이름에 맞는 박스 그리기 색을 정한다. | 직접 `CameraThread._draw_tools` · `CameraThread._draw_yolo` | `DETECT_BOX_COLORS` `DETECT_BOX_FALLBACK` `TOOL_BOX_COLORS` |
| `CameraThread.__init__` | 189 | 카메라 스레드의 연결·손 검출·공구 검출 준비를 한다. | 직접 `safety_console.SafetyConsole.__init__` | `CAMERA_TCP_HOST` `HAND_ROI_RING_PX_VGA` `SHOW_DETECT_BOXES` `TOOL_ENABLED` |
| `CameraThread.set_tool_scan` | 228 | 공구 지참 단계 동안 공구 인식을 켜고 끈다. | 직접 `safety_console.SafetyConsole._begin_sub` · `safety_console.SafetyConsole._end_tool_scan` | — |
| `CameraThread.set_active` ⚠️ | 247 | 버튼 검출을 멈추거나 다시 켠다. | **없음** | — |
| `CameraThread.set_draw_boxes` ⚠️ | 253 | 화면에 검출 박스·손 표시를 그릴지 정한다. | 직접 `safety_console.SafetyConsole.__init__` · `safety_console.SafetyConsole._on_boxes_changed` | — |
| `CameraThread.draw_boxes` ⚠️ | 258 | 지금 검출 박스를 그리고 있는지 알려준다. | **없음** | — |
| `CameraThread.set_frame_sink` | 262 | 시연영상 촬영기가 매 프레임을 받도록 연결한다. | 직접 `safety_console.SafetyConsole._start_demo_capture` · `safety_console.SafetyConsole._stop_demo_capture` | — |
| `CameraThread.retry_connect` | 271 | 포기한 카메라 연결을 사용자가 다시 시도하게 한다. | 이름 후보 `safety_console.SafetyConsole._on_retry` | — |
| `CameraThread.gave_up` | 282 | 카메라 자동 재연결을 포기했는지 알려준다. | **없음** | — |
| `CameraThread.set_host` ⚠️ | 285 | 연결할 카메라 IP 주소를 바꾼다. | **없음** | — |
| `CameraThread._init_calibration` | 292 | 프레임 크기에 맞는 렌즈 보정과 링 두께를 정한다. | 직접 `CameraThread._ensure_calibration` | — |
| `CameraThread._ensure_calibration` | 303 | 프레임 크기가 바뀌면 렌즈 보정을 다시 정한다. | 직접 `CameraThread.run` | — |
| `CameraThread._undistort` ⚠️ | 313 | 카메라 렌즈의 휘어짐을 펴 준다. | 직접 `CameraThread._process_frame` | — |
| `CameraThread._draw_yolo` | 321 | 검출된 버튼 박스와 이름·점수를 영상에 그린다. | 직접 `CameraThread._process_frame` | — |
| `CameraThread._draw_tools` | 333 | 검출된 공구 박스를 영상에 그린다(표시용). | 직접 `CameraThread._process_frame` | `TOOL_SCAN_INTERVAL_SEC` |
| `CameraThread._recv_worker` | 406 | 자기 연결의 소켓에서 받은 최신 프레임을 받은 시각과 함께 보관한다 — 자기 연결일 때만 알린다. (④ C19·C16) | 연결 `CameraThread.run` | — |
| `CameraThread._frame_error` | 426 | 프레임 처리 오류를 적는다 — 그 프레임만 버리고 로그는 5초에 한 번. (④ C12) | 직접 `CameraThread.run` | — |
| `_close_sock` | 62 | 소켓을 shutdown 한 뒤 닫는다 — 다른 스레드의 막힌 수신을 깨운다. (④ C19) | 직접 `CameraThread.run` · `CameraThread.stop` | — |
| `CameraThread.run` ⚠️ | 371 | 카메라 연결·재연결과 프레임 처리를 반복한다. | 이름 후보 `demo_ffmpeg._screen_size` · `demo_recorder.keep_screen_awake` | `CONNECT_MAX_TRIES` `TCP_RECONNECT_DELAY_SEC` `TCP_RECV_TIMEOUT_SEC` |
| `CameraThread._process_frame` ⚠️ | 467 | 한 프레임에서 버튼·손·공구를 찾아 결과를 알린다. | 직접 `CameraThread.run` | `TOOL_SCAN_INTERVAL_SEC` |
| `CameraThread._connect_tcp` ⚠️ | 548 | ESP32 카메라에 TCP로 연결한다. | 직접 `CameraThread.run` | `CAMERA_TCP_PORT` `TCP_RECV_TIMEOUT_SEC` |
| `CameraThread._recv_latest_frame` | 564 | 밀린 프레임은 버리고 가장 최근 프레임 하나를 받는다. | 직접 `CameraThread._recv_worker` | `TCP_MAX_FRAME_BYTES` |
| `CameraThread._recv_exact` | 584 | 정해진 바이트 수만큼 빠짐없이 받는다. | 직접 `CameraThread._recv_latest_frame` | — |
| `CameraThread.stop` | 602 | 카메라 스레드를 멈추고 연결을 닫는다. | 이름 후보 `anim.Pulse.stop` · `anim.TextPulse.stop` 외 13 | — |
| `_one_per_class.rank` | 138 | 같은 클래스 트랙을 겨룰 순서(지금 보임 → 오래됨 → 점수)를 만든다. (③ R2) | 직접 `_one_per_class` | — |
| `CameraThread._on_connected` | 245 | (재)연결 직후 끊기기 전 트랙을 버리고 재연결 신호를 보낸다. (③ R5) | 직접 `CameraThread.run` | — |

### `gpio_input.py` (6개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `GpioInputController.__init__` ⚠️ | 37 | 물리 버튼 B1~B4·비상정지 핀을 연결해 둔다. | 직접 `safety_console.SafetyConsole.__init__` | `GPIO_BUTTON_PINS` `GPIO_EMO_PIN` `GPIO_INPUT_ENABLED` |
| `GpioInputController.available` | 91 | 버튼·비상정지 입력이 모두 준비됐는지 알려준다. | 이름 후보 `camera_thread.CameraThread.run` · `hand_tracker(최상위)` 외 1 | — |
| `GpioInputController.reason` | 102 | 입력이 준비되지 않은 이유를 알려준다. | 직접 `hand_tracker(최상위)` · `safety_console.SafetyConsole.__init__` | — |
| `GpioInputController.emo_active` | 106 | 비상정지가 지금 눌려 있는지 확인한다. | 직접 `safety_console.SafetyConsole._refuse_reset_if_emo` · `safety_console.SafetyConsole._release_block` | — |
| `GpioInputController._fire` | 119 | 버튼 신호를 프로그램 쪽으로 전달한다. | 직접 `GpioInputController.__init__` | — |
| `GpioInputController.close` | 126 | 모든 버튼 핀 연결을 해제한다. | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 12 | — |

### `serial_ports.py` (2개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `resolve` ⚠️ | 17 | USB 신원으로 원하는 시리얼 장치 경로를 찾는다. | 직접 `serial_ports(최상위)` | — |
| `describe` ⚠️ | 30 | 꽂혀 있는 시리얼 장치 목록을 글로 보여준다. | 직접 `serial_ports(최상위)` | — |

### `detector.py` (11개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `BaseDetector.detect` | 19 | 검출기가 갖춰야 할 '프레임 검출' 틀이다. | 이름 후보 `camera_thread.CameraThread._process_frame` · `hand_tracker(최상위)` 외 1 | — |
| `BaseDetector.class_name` | 26 | 검출기가 갖춰야 할 '번호→이름' 틀이다. | 이름 후보 `camera_thread.CameraThread._draw_yolo` · `camera_thread.CameraThread._process_frame` 외 1 | — |
| `BaseDetector.close` | 30 | 검출기 정리용 빈 기본 동작이다. | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 13 | — |
| `PyTorchDetector.__init__` ⚠️ | 40 | CPU용 버튼 검출 모델(best.pt)을 불러온다. | 직접 `create_detector` | `PT_MODEL_PATH` |
| `PyTorchDetector.detect` ⚠️ | 50 | CPU 모델로 프레임 속 버튼을 찾는다. | 이름 후보 `camera_thread.CameraThread._process_frame` · `hand_tracker(최상위)` 외 1 | `YOLO_CONF_LOW` `YOLO_INPUT_SIZE` |
| `PyTorchDetector.class_name` | 65 | CPU 모델의 클래스 번호를 버튼 이름으로 바꾼다. | 이름 후보 `camera_thread.CameraThread._draw_yolo` · `camera_thread.CameraThread._process_frame` 외 1 | — |
| `HailoDetector.__init__` ⚠️ | 81 | Hailo 칩에 버튼 검출 모델을 올리고 준비한다. | 직접 `create_detector` | `HEF_MODEL_PATH` |
| `HailoDetector.detect` | 105 | Hailo 칩으로 프레임 속 버튼 위치를 찾는다. | 이름 후보 `camera_thread.CameraThread._process_frame` · `hand_tracker(최상위)` 외 1 | `YOLO_CONF_LOW` `YOLO_INPUT_SIZE` |
| `HailoDetector.class_name` | 133 | Hailo 모델의 클래스 번호를 버튼 이름으로 바꾼다. | 이름 후보 `camera_thread.CameraThread._draw_yolo` · `camera_thread.CameraThread._process_frame` 외 1 | — |
| `HailoDetector.close` | 136 | 자기 추론 통로만 닫는다(공유 장치는 둔다). | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 12 | — |
| `create_detector` | 145 | 설정에 맞는 버튼 검출기를 만든다. | **없음** | `INFERENCE_BACKEND` |

### `hailo_device.py` (3개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `get_vdevice` | 50 | 여러 모델이 함께 쓰는 Hailo 장치를 하나 만든다. | 직접 `configure` · `hand_tracker._SharedInference.__init__` | — |
| `configure` | 66 | 모델 파일을 공유 Hailo 장치에 올린다. | 직접 `detector.HailoDetector.__init__` | — |
| `shutdown` ⚠️ | 89 | 공유 Hailo 장치를 해제한다. | **없음** | — |

### `hand_tracker.py` (11개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_prepare_paths` | 33 | 손 검출 외부 소스 폴더를 불러올 경로에 넣는다. | 직접 `HandTracker.__init__` · `_SharedInference.__getattr__` · `_SharedInference._configure_and_get_network_group` | `HAND_BLAZE_DIR` |
| `_NoActivateGroup.__init__` | 62 | 모델 묶음을 감싸 보관한다. | 이름 후보 `camera_thread.CameraThread.__init__` · `overlay.AlertBanner.__init__` 외 14 | — |
| `_NoActivateGroup.activate` | 65 | 불필요한 활성화 호출을 조용히 무시한다. | 직접 `safety_console.SafetyConsole._toggle_settings` · `safety_console.SafetyConsole._toggle_sheet` | — |
| `_NoActivateGroup.__getattr__` | 68 | 나머지 요청은 원래 모델 묶음에 넘긴다. | 객체 생성·파이썬 | — |
| `_SharedInference.__init__` | 80 | 손 모델이 공유 Hailo 장치를 쓰도록 준비한다. | 이름 후보 `camera_thread.CameraThread.__init__` · `overlay.AlertBanner.__init__` 외 14 | — |
| `_SharedInference._configure_and_get_network_group` | 90 | 손 모델을 장치에 올리고 감싸서 돌려준다. | **없음** | — |
| `_SharedInference.__getattr__` | 98 | 없는 기능은 외부 손 검출 코드에서 빌려 쓴다. | 객체 생성·파이썬 | — |
| `HandTracker.__init__` ⚠️ | 108 | 손바닥·손가락 검출 모델을 불러 준비한다. | 직접 `camera_thread.CameraThread.__init__` · `hand_tracker(최상위)` | `HAND_BLAZE_DIR` `HAND_MODELS_DIR` |
| `HandTracker.detect` | 150 | 프레임에서 검지 끝 위치를 찾는다. | 이름 후보 `camera_thread.CameraThread._process_frame` · `hand_tracker(최상위)` 외 1 | `HAND_MIN_SCORE` |
| `HandTracker.close` ⚠️ | 191 | 손 검출기를 끄고 모델 참조를 놓는다. | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 13 | — |
| `pick_hand` | 31 | 여러 손 중 신뢰도가 가장 높은 손 하나의 랜드마크를 고른다. (③ R1) | 직접 `HandTracker.detect` | — |

### `frame_orient.py` (11개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `flip` | 31 | 거꾸로 달린 카메라 영상을 위아래로 뒤집는다. | 직접 `camera_thread.CameraThread._process_frame` · `apply` · `apply_full` | `CAMERA_FLIP_VERTICAL` |
| `rotate` | 38 | 옆으로 달린 카메라 영상을 반시계 90도 돌린다. | 직접 `camera_thread.CameraThread._process_frame` · `apply` · `apply_full` | `CAMERA_ROTATE_CCW90` |
| `apply` | 45 | 영상 뒤집기와 회전을 한 번에 한다(도구용). | **없음** | — |
| `calibration_path` | 50 | 해상도에 맞는 렌즈 보정 파일을 찾는다. | 직접 `load_undistort` | `YOLO_CALIBRATION_PATH` |
| `load_undistort` | 71 | 렌즈 보정 파일로 휘어짐 펴기 지도를 만든다. | 직접 `camera_thread.CameraThread._init_calibration` · `_selftest` · `undistort_map` | `CALIB_ALPHA` |
| `undistort_map` ⚠️ | 87 | 휘어짐 펴기 지도만 돌려준다(옛 호출 호환). | **없음** | — |
| `px_scale` | 92 | VGA 기준 픽셀 값을 현재 해상도에 맞출 배율을 낸다. | 직접 `ring_px` | — |
| `ring_px` | 97 | 해상도에 맞춘 버튼 테두리 링 두께를 낸다. | 직접 `camera_thread.CameraThread._init_calibration` | `HAND_ROI_RING_PX_VGA` |
| `undistort` | 102 | 지도로 영상의 렌즈 휘어짐을 편다. | 직접 `apply_full` | — |
| `apply_full` | 109 | 뒤집기→휘어짐 펴기→회전을 순서대로 한다. | 직접 `_selftest` | — |
| `_selftest` ⚠️ | 114 | 보정 파일별로 방향 보정 결과 크기를 점검한다. | **없음** | `CAMERA_ROTATE_CCW90` |

### `roi_zones.py` (1개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `zone_at_point` | 29 | 손끝이 어느 버튼의 박스 안·링에 있는지 판정한다. | 직접 `camera_thread.roi_at_point` · `camera_thread.zone_at_point` | — |

### `tool_gate.py` (11개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `ToolGate.__init__` | 42 | 공구 인식 워커 설정(폴더·모델·임계)을 준비한다. | 직접 `camera_thread.CameraThread.__init__` | `TOOL_CONF` `TOOL_MODEL_PATH` `TOOL_SHM_DIR` `TOOL_WORKER_PYTHON` |
| `ToolGate.start` | 64 | 공구 인식 워커 프로세스를 띄운다. | 이름 후보 `anim.tween` · `camera_thread.CameraThread.run` 외 13 | — |
| `ToolGate.stop` | 95 | 공구 인식 워커 프로세스를 내린다. | 이름 후보 `anim.Pulse.stop` · `anim.TextPulse.stop` 외 13 | — |
| `ToolGate.available` | 112 | 공구 인식 워커가 준비됐는지 알려준다(잠금을 잡고 `_ready`). | **없음**(시험만 — 내부는 `_ready` 를 직접 부른다 · ④ C13) | — |
| `ToolGate.request` | 117 | 프레임을 공구 인식 워커에 보내고 손끝을 기억한다. | 직접 `camera_thread.CameraThread._process_frame` · `voice_llm.ask` | — |
| `ToolGate.poll` | 142 | 새 공구 인식 결과가 있으면 가져온다. | 직접 `camera_thread.CameraThread._process_frame` · `demo_ffmpeg.FfmpegSet.start` | — |
| `ToolGate._start_locked` | 74 | (잠금 안에서) 워커를 띄운다 — 오류 출력은 `worker.err` 로. (④ C13·C14) | 직접 `ToolGate.start` | — |
| `ToolGate._ready` | 133 | (잠금 안에서) 워커가 살아 있고 모델 로딩까지 끝났는지 — 죽었으면 한 번 로그하고 내린 것으로 본다. (④ C14) | 직접 `ToolGate.available` · `ToolGate._request_locked` · `ToolGate._poll_locked` | — |
| `ToolGate._err_tail` | 151 | 워커 오류 출력의 끝 몇 줄을 가져온다. (④ C14) | 직접 `ToolGate._ready` | — |
| `ToolGate._request_locked` | 171 | (잠금 안에서) 프레임을 워커에 보내고 손끝을 기억한다. (④ C13) | 직접 `ToolGate.request` | — |
| `ToolGate._poll_locked` | 199 | (잠금 안에서) 새 결과를 가져온다. (④ C13) | 직접 `ToolGate.poll` | — |

### `tool_worker.py` (3개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_write_atomic` | 45 | 파일을 반쯤 쓴 상태가 보이지 않게 저장한다. | 직접 `main` | — |
| `_latest_request` | 52 | 가장 최근 요청 프레임만 고르고 나머지는 지운다. | 직접 `main` | — |
| `main` ⚠️ | 77 | 공구 모델로 요청 프레임을 계속 인식해 결과를 쓴다. | 직접 `tool_worker(최상위)` | — |

### `tool_state.py` (8개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_base` | 44 | 'wrench-in-hand' 같은 이름을 'wrench'로 바꾼다. | 직접 `ToolState._in_hand_tool` · `ToolState.update` | — |
| `ToolState.__init__` | 57 | 필요한 공구와 '찾는 중' 상태로 시작한다. | 직접 `safety_console.SafetyConsole._begin_sub` | — |
| `ToolState.phase` | 63 | 지금 찾는 중인지 쥐었는지 알려준다. | 직접 `safety_console.SafetyConsole._on_tool` | — |
| `ToolState.want_tool` | 68 | 이번 단계에 필요한 공구를 알려준다. | 연결 `ToolState.__init__` | — |
| `ToolState.update` ⚠️ | 72 | 공구 검출 결과로 필요한 공구를 쥐었는지 판정한다. | 직접 `overlay.ConnBar.update_state` · `safety_console.SafetyConsole._on_tool` 외 2 | — |
| `ToolState._in_hand_tool` ⚠️ | 110 | 모델이 '손에 쥠'으로 본 공구를 고른다. | 직접 `ToolState.update` | — |
| `ToolState._held_tool` | 124 | 손끝이 들어 있는 공구 박스를 고른다. | 직접 `ToolState.update` | — |
| `ToolState.force_grasped` | 71 | 시연용 `t` 키로 공구를 쥔 것으로 확정한다(서브 작업이 끝날 때까지 유지). (② G9) | 직접 `safety_console.SafetyConsole._sim_tool_grasped` | — |

### `fsm.py` (16개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `SafetyFSM.__init__` | 44 | 작업 순서 판정기의 단계·임계값·알림 연결을 준비한다. | 직접 `safety_console.SafetyConsole.__init__` | `FSM_DWELL_THRESHOLD_SEC` `FSM_EMO_BUTTON` `FSM_STEP_COUNT` |
| `SafetyFSM.correct_roi` | 88 | 지금 눌러야 할 정답 버튼 이름을 알려준다. | 연결 `SafetyFSM.current_step_name` · `SafetyFSM.press_button` 외 1 / 직접 `safety_console.SafetyConsole._commit_button` · `safety_console.SafetyConsole._on_fsm_state` 외 3 | — |
| `SafetyFSM.last_roi` | 95 | 손이 마지막으로 머문 버튼을 알려준다. | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |
| `SafetyFSM.current_step_name` | 104 | 지금 단계의 사람용 이름을 알려준다. | 직접 `safety_console.SafetyConsole._commit_button` · `safety_console.SafetyConsole._on_fsm_state` 외 2 | — |
| `SafetyFSM._goto` | 110 | 상태를 바꾸고 경고·차단 신호를 함께 보낸다. | 직접 `SafetyFSM._emo_stop` · `SafetyFSM._step_complete` · `SafetyFSM.load_recipe` 외 5 | — |
| `SafetyFSM._reset_dwell` | 127 | 오답 버튼 위 머문 시간 측정을 초기화한다. | 직접 `SafetyFSM._emo_stop` · `SafetyFSM._step_complete` · `SafetyFSM.press_button` 외 2 | — |
| `SafetyFSM.load_recipe` | 132 | 작업 시작 시 대기에서 진행 상태로 넘긴다. | 이름 후보 `safety_console.SafetyConsole._on_start_process` | — |
| `SafetyFSM.update_vision` ⚠️ | 139 | 손 위치를 받아 오답 버튼에 오래 머물면 경고한다. | 직접 `safety_console.SafetyConsole._on_roi` | — |
| `SafetyFSM.press_button` ⚠️ | 216 | 실제 버튼 눌림을 받아 다음 단계·차단을 정한다. | 직접 `safety_console.SafetyConsole._commit_button` | — |
| `SafetyFSM._step_complete` | 236 | 정답 처리 후 다음 단계로, 마지막이면 완료로 간다. | 직접 `SafetyFSM.press_button` | — |
| `SafetyFSM._emo_stop` | 248 | 비상정지 버튼이 눌리면 즉시 차단 상태로 간다. | 직접 `SafetyFSM.press_button` | — |
| `SafetyFSM.reset` | 258 | 작업을 처음(대기·1단계)으로 되돌린다. | 이름 후보 `safety_console.SafetyConsole._reset_work` · `voice_assistant.run` | — |
| `SafetyFSM.release_warning` | 274 | 경고를 풀고 감시 상태로 돌아간다. | 직접 `safety_console.SafetyConsole._on_alert_release` | — |
| `SafetyFSM.release_block` | 279 | 차단을 푼다 — 위반 차단은 작업을 다시 진행 상태로, EMO 차단은 「작업 시작」 전 대기(IDLE)로 둔다(판정 수정 ①). | 직접 `safety_console.SafetyConsole._release_block` | — |
| `SafetyFSM.emo_active` | 277 | 지금 차단이 EMO 로 걸린 것인지 알려 준다. (③ V2) | 직접 `safety_console.SafetyConsole._publish_state` | — |
| `SafetyFSM.forget_observation` | 300 | 카메라 재연결 때 체류·관측 기록을 지운다(방금 완료한 버튼 예외는 유지). (③ R5) | 직접 `safety_console.SafetyConsole._on_stream_reset` | — |

### `recipe.py` (3개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `load_recipe` | 19 | 공정 레시피 JSON 파일을 읽고 검사해 돌려준다. | 직접 `safety_console.SafetyConsole.__init__` / 직접 `recipe(최상위)` | — |
| `_validate` ⚠️ | 33 | 레시피의 단계 순서·버튼 중복·EMO 겹침·체류시간이 맞는지 검사한다. | 직접 `load_recipe` | — |
| `_validate_sub` | 64 | 단계별 서브 작업(대기·공구)의 종류·시간·공구 목록을 검사한다. | 직접 `_validate` | — |

### `sub_task.py` (20개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `SubTask.__init__` | 31 | 한 단계의 서브 작업을 시작 시각과 함께 준비한다. | 직접 `safety_console.SafetyConsole._begin_sub` | — |
| `SubTask.is_active` | 43 | 진행할 서브 작업이 있는 단계인지 알려 준다. | 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._press_button` 외 3 | — |
| `SubTask.needs_tool` | 48 | 이 서브 작업이 공구를 쥐어야 하는 종류인지 알려 준다. | 연결 `SubTask.can_advance` · `SubTask.tool_ok` 외 2 / 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._sim_tool_grasped` | — |
| `SubTask.label` | 52 | 게이지에 보일 서브 작업 이름을 돌려준다. | 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._press_button` | — |
| `SubTask.total_sec` | 56 | 서브 작업에 필요한 전체 대기 시간(초)을 돌려준다. | 연결 `SubTask.progress` / 직접 `overlay.GaugePanel.update_view` | — |
| `SubTask.elapsed_sec` | 60 | 서브 작업이 시작된 뒤 지난 시간(초)을 돌려준다. | 연결 `SubTask.progress` / 직접 `overlay.GaugePanel.update_view` | — |
| `SubTask.progress` | 66 | 대기 진행률을 0~1 사이 값으로 돌려준다. | 연결 `SubTask.time_done` / 직접 `overlay.GaugePanel.update_view` · `overlay.GaugePanel.update_view._step` | — |
| `SubTask.time_done` | 73 | 대기 시간이 다 찼는지 알려 준다. | 연결 `SubTask.can_advance` / 직접 `overlay.GaugePanel.update_view` | — |
| `SubTask.want_tool` | 78 | 이 단계가 요구하는 공구 이름(키)을 돌려준다. | 연결 `SubTask.tool_ok` · `SubTask.want_tool_name` 외 1 | — |
| `SubTask.want_tool_name` | 83 | 요구 공구의 한글 표시 이름을 돌려준다. | 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._sim_tool_grasped` 외 1 | — |
| `SubTask.tool_ok` | 87 | 요구한 공구를 제대로 쥐었는지 알려 준다. | 연결 `SubTask.can_advance` / 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._sim_tool_grasped` | — |
| `SubTask.wrong_tool` | 91 | 다른 공구를 쥐고 있으면 그 공구를 돌려준다. | 연결 `SubTask.wrong_tool_name` / 직접 `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._update_sub_view` | — |
| `SubTask.wrong_tool_name` | 101 | 잘못 쥔 공구의 한글 표시 이름을 돌려준다. | 직접 `safety_console.SafetyConsole._update_sub_view` | — |
| `SubTask._tool_name` | 104 | 공구 키를 레시피의 한글 이름으로 바꿔 준다. | 직접 `SubTask.want_tool_name` · `SubTask.wrong_tool_name` | — |
| `SubTask.can_advance` | 112 | 시간과 공구 조건이 채워져 다음 단계로 넘어갈 수 있는지 판단한다. | 직접 `safety_console.SafetyConsole._update_sub_view` | — |
| `SubTask.tick` | 126 | 서브 작업의 현재 시각을 갱신한다. | 직접 `safety_console.SafetyConsole._tick_sub` | — |
| `SubTask.set_tool` | 130 | 손에 쥔 것으로 판정된 공구를 기록한다. | 직접 `safety_console.SafetyConsole._on_tool` · `safety_console.SafetyConsole._sim_tool_grasped` | — |
| `SubTask.paused` | 81 | 서브 작업이 경고로 멈춰 있는지 알려 준다. (② G1) | 직접 `SubTask.can_advance` · `overlay.GaugePanel.update_view` · `safety_console.SafetyConsole._on_fsm_state` | — |
| `SubTask.pause` | 141 | 서브 작업을 멈춘다 — 멈춘 동안의 시간은 경과에서 뺀다. (② G1) | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |
| `SubTask.resume` | 146 | 멈춘 서브 작업을 남은 시간부터 잇는다. (② G1) | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |

### `interlock.py` (16개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `InterlockController.__init__` | 49 | 아두이노 인터록 연결과 전송·재연결 스레드를 준비한다. | 직접 `safety_console.SafetyConsole.__init__` | `INTERLOCK_BAUD` `INTERLOCK_ENABLED` `INTERLOCK_PORT` `INTERLOCK_TIMEOUT` |
| `InterlockController._open` ⚠️ | 98 | 아두이노 시리얼 포트를 연다. | 직접 `InterlockController.__init__` · `InterlockController._reconnect_loop` | — |
| `InterlockController._sync_after_open` | 119 | 연결 직후 램프 상태를 현재 상태로 맞춘다. | 직접 `InterlockController.__init__` · `InterlockController._reconnect_loop` | — |
| `InterlockController._reconnect_loop` | 130 | 끊긴 인터록을 주기적으로 다시 연결한다. | 연결 `InterlockController.__init__` | — |
| `InterlockController.retry_connect` | 161 | 포기한 인터록 연결을 다시 시도하게 한다. | 이름 후보 `safety_console.SafetyConsole._on_retry` | — |
| `InterlockController.gave_up` ⚠️ | 168 | 인터록 자동 재연결을 포기했는지 알려준다. | **없음** | — |
| `InterlockController.connected` | 172 | 인터록이 연결돼 있는지 알려준다 — 열림 · 장치 노드 있음 · **마지막 명령에 ACK**(③ I2 · 최종 리뷰 I-3). | 직접 `safety_console.SafetyConsole._show_block_banner` · `safety_console.SafetyConsole._update_conn_bar` / 이름 후보 `precheck.run_stage1`(getattr) | — |
| `InterlockController._link_ok` | 216 | 핸들이 열려 있고 장치 노드가 있는지(ACK 와 무관) — 재연결 루프의 링크 판단. (③ 최종 리뷰 I-3) | 직접 `InterlockController.connected` · `InterlockController._reconnect_loop` | — |
| `InterlockController._write` | 185 | 보낼 명령을 전송 대기열에 넣는다. | 직접 `InterlockController._sync_after_open` · `InterlockController.set_feedback` · `InterlockController.set_interlock` | — |
| `InterlockController._writer_loop` | 194 | 대기열의 명령을 하나씩 꺼내 보낸다. | 연결 `InterlockController.__init__` | — |
| `InterlockController._write_now` | 206 | 명령을 아두이노에 보내고 응답(ACK)을 확인한다. | 직접 `InterlockController._writer_loop` | — |
| `InterlockController._fault` | 251 | 차단이 확인 안 될 때 경보를 알린다. | 직접 `InterlockController._write_now` | — |
| `InterlockController._drop` | 260 | 현재 시리얼 연결을 닫고 비운다. | 직접 `InterlockController._write_now` · `InterlockController.close` | — |
| `InterlockController.set_interlock` | 270 | 차단이 걸리면 즉시 BLOCK 명령을 보낸다. | 직접 `safety_console.SafetyConsole._on_interlock` | — |
| `InterlockController.set_feedback` | 280 | 경고 단계에 맞는 램프 명령을 보낸다. | 직접 `safety_console.SafetyConsole._on_feedback` | — |
| `InterlockController.close` | 293 | 인터록 스레드를 멈추고 포트를 닫는다. | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 13 | — |

### `safety_console.py` (80개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `SafetyConsole.__init__` | 63 | 레시피·FSM·인터락·버튼입력·카메라를 준비하고 시작 로그를 남긴다. | 직접 `main.main` | `CAMERA_TCP_HOST` `CAMERA_TCP_PORT` `DEMO_CAPTURE` `DEMO_CAPTURE_DIR` 외 10 |
| `SafetyConsole._init_ui` ⚠️ | 219 | 영상·상태판·게이지·메뉴·알림·결과창 등 화면 요소를 만들고 연결한다. | 직접 `SafetyConsole.__init__` | `RECORDING_SAVE_DIR` `SHOW_FPS` `font` |
| `SafetyConsole._relayout` | 355 | 창 크기에 맞춰 영상과 모든 패널·버튼 위치를 다시 잡는다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._on_fsm_state` · `SafetyConsole._on_panel_bg_changed` 외 4 | `SHOW_FPS` |
| `SafetyConsole.resizeEvent` | 406 | 창 크기가 바뀌면 화면 배치를 다시 한다. | 이름 후보 `overlay_menu.NotifyButton.resizeEvent` | — |
| `SafetyConsole._apply_theme` | 410 | 현재 테마(다크/화이트) 색을 모든 패널·버튼에 칠한다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._on_panel_bg_changed` · `SafetyConsole._on_theme_changed` | — |
| `SafetyConsole._update_camera_frame` | 441 | 새 카메라 프레임을 기록하고 화면에 영상을 보여 준다. | 직접 `SafetyConsole.__init__` | `DEMO_HIDE_VIDEO` |
| `SafetyConsole._fit_to_label` | 450 | 카메라 영상을 화면 칸 크기에 맞게 늘리거나 잘라 준다. | 직접 `SafetyConsole._update_camera_frame` | `CAMERA_DISPLAY_FILL` |
| `SafetyConsole._note_frame` | 471 | 프레임 도착 시각·크기를 적어 FPS·점검·녹화에 쓰게 한다. | 직접 `SafetyConsole._update_camera_frame` | `DEMO_CAPTURE` |
| `SafetyConsole._toggle_menu` | 509 | 메뉴 창을 열거나 닫는다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._open_check` · `SafetyConsole._open_record` 외 3 | — |
| `SafetyConsole._toggle_notify` | 519 | 알림 창을 열거나 닫고, 열면 안 읽은 수를 지운다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._toggle_settings` | 531 | 설정 창을 열거나 닫고, 작업 중이면 공구 변경을 막는다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._open_settings` | — |
| `SafetyConsole._sheets` | 549 | 화면에 겹쳐 뜨는 창(시트) 목록을 돌려준다. | 직접 `SafetyConsole._any_sheet_open` · `SafetyConsole._close_sheets` · `SafetyConsole._dim_others` 외 1 | — |
| `SafetyConsole._close_sheets` ⚠️ | 553 | 지정한 하나만 빼고 열린 창과 로그 창을 모두 닫는다. | 직접 `SafetyConsole._show_result` · `SafetyConsole._toggle_menu` · `SafetyConsole._toggle_notify` 외 2 | — |
| `SafetyConsole._any_sheet_open` | 561 | 열려 있는 창이 하나라도 있는지 알려 준다. | 직접 `SafetyConsole._close_result` · `SafetyConsole._sync_cta_visibility` · `SafetyConsole._toggle_menu` 외 3 | — |
| `SafetyConsole._sync_cta_visibility` | 564 | 가운데 「작업 시작」 버튼을 보일지 지금 상태로 정한다. | 직접 `SafetyConsole._close_result` · `SafetyConsole._on_fsm_state` · `SafetyConsole._reset_work` 외 6 | — |
| `SafetyConsole._open_check` | 585 | 메뉴에서 연결 점검 창을 열고 점검을 바로 돌린다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._open_record` | 591 | 메뉴에서 녹화 창을 열고 현재 녹화 상태를 보여 준다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._open_recordings_folder` | 598 | 녹화 저장 폴더를 파일관리자로 연다. | 직접 `SafetyConsole._init_ui` | `RECORDING_SAVE_DIR` |
| `SafetyConsole._run_manual_check` | 612 | 연결 점검(1차)을 지금 다시 돌려 결과를 보여 준다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._open_check` / 연결 `SafetyConsole._on_retry` | — |
| `SafetyConsole._update_conn_bar` | 616 | 1초마다 카메라·인터락·버튼 연결 상태와 FPS를 갱신한다. | 직접 `SafetyConsole._init_ui` | `SHOW_FPS` `UI_ANIMATION` |
| `SafetyConsole._check_ctx` | 638 | 점검에 필요한 카메라·인터락·최근 프레임 등을 한데 모은다. | 직접 `SafetyConsole._run_manual_check` · `SafetyConsole._update_conn_bar` | — |
| `SafetyConsole._frame_for_check` | 657 | 점검용으로 최근 화면을 이미지 배열로 바꿔 준다. | 직접 `SafetyConsole._check_ctx` | — |
| `SafetyConsole._on_retry` | 670 | 점검 창의 재연결 요청을 카메라·인터락에 전하고 다시 점검한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._toggle_sheet` | 682 | 점검·녹화 같은 창을 열거나 닫고 크기를 다시 맞춘다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole._open_check` · `SafetyConsole._open_record` | — |
| `SafetyConsole._open_settings` | 698 | 메뉴를 닫고 설정 창을 연다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._set_log_visible` | 702 | 로그 창과 그 닫기 버튼을 함께 보이거나 숨긴다. | 직접 `SafetyConsole._close_sheets` · `SafetyConsole._init_ui` · `SafetyConsole._show_log` 외 1 | — |
| `SafetyConsole._show_log` | 711 | 메뉴에서 로그 창을 열거나 닫는다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._dim_others` | 745 | 창이 열리거나 경고가 뜨면 뒤 화면을 어둡게 가린다. | 직접 `SafetyConsole._refresh_dim`(④ U13 — 켜고 끌지는 거기서 계산) | — |
| `SafetyConsole._refresh_dim` | 735 | 열린 창이나 배너가 있으면 뒤 화면을 어둡게 — 지금 상태에서 계산한다. (④ U13) | 직접 `SafetyConsole._toggle_menu` · `_toggle_notify` · `_toggle_settings` · `_toggle_sheet` · `_show_result` · `_close_result` · `_update_sub_view` · `_reset_work` · `_on_fsm_state` | — |
| `SafetyConsole._popup` | 1522 | 종류마다 알림 창 하나를 띄운다 — 이미 있으면 글자만 바꿔 앞으로. (④ U20) | 직접 `SafetyConsole._release_block` · `_refuse_reset_if_emo` · `_on_interlock_fault` | — |
| `SafetyConsole._wire_tool_settings` | 746 | 레시피의 공구 목록을 설정 창 선택지로 넘긴다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._on_tool_changed` | 759 | 설정에서 고른 지정 공구를 이번 실행 동안 적용한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._on_theme_changed` | 765 | 화면 테마를 바꾸고 모든 화면에 다시 칠한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._on_panel_bg_changed` | 771 | 단계·게이지 패널 배경을 켜거나 끈다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._on_boxes_changed` ⚠️ | 778 | 화면의 검출 박스 표시를 켜거나 끈다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._tool_display_name` | 783 | 공구 키를 레시피의 화면용 이름으로 바꾼다. | 직접 `SafetyConsole._on_tool_changed` | — |
| `SafetyConsole._on_connect_gave_up` | 791 | 카메라·인터락 연결을 포기하면 알림 한 건으로 알린다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._notify` | 796 | 알림을 추가하고 안 읽은 수 배지를 올린다. | 직접 `SafetyConsole._commit_button` · `SafetyConsole._on_connect_gave_up` · `SafetyConsole._on_fsm_state` 외 6 | — |
| `SafetyConsole._append_log` | 803 | 시각을 붙여 로그 창과 로그 파일에 한 줄 적는다. | 직접 `SafetyConsole.__init__` · `SafetyConsole._begin_sub` · `SafetyConsole._commit_button` 외 29 | — |
| `SafetyConsole._on_yolo_detections` | 817 | 버튼 검출 결과를 기억·로그하고 결과 집계에 한 프레임으로 센다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_hand` | 845 | 이번 프레임에 손이 보였는지 기억한다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_roi` | 852 | 손끝이 든 버튼 구역을 FSM에 넘기고 바뀌면 로그한다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_tool` | 866 | 공구 검출 결과로 공구 판정을 갱신해 서브 작업에 넘긴다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._sim_tool_grasped` | 894 | 시연용 T 키로 요구 공구를 쥔 것으로 처리한다. | 직접 `SafetyConsole.keyPressEvent` | — |
| `SafetyConsole._on_start_process` | 921 | 작업을 시작해 FSM·결과 집계를 첫 단계로 맞춘다. | 직접 `SafetyConsole._on_cta` | — |
| `SafetyConsole._press_button` | 934 | 버튼 눌림을 받아 서브 작업을 시작하거나 FSM에 전한다. | 직접 `SafetyConsole.__init__` · `SafetyConsole.keyPressEvent` | — |
| `SafetyConsole._commit_button` | 984 | 버튼 눌림을 FSM에 실제로 전하고 단계 진행·완료를 처리한다. | 직접 `SafetyConsole._finish_sub` · `SafetyConsole._press_button` | — |
| `SafetyConsole._show_result` | 1000 | 작업이 끝나면 결과를 집계해 결과창을 띄운다. | 직접 `SafetyConsole._commit_button` | — |
| `SafetyConsole._close_result` | 1011 | 결과창을 닫고 화면 어둡게·시작 버튼을 정리한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._sub_spec_for` | 1016 | 그 버튼 단계의 서브 작업(대기·공구) 내용을 찾아 준다. | 직접 `SafetyConsole._press_button` | — |
| `SafetyConsole._publish_state` ⚠️ | 1026 | 음성비서가 읽을 현재 작업 상태를 파일로 내보낸다. | 직접 `SafetyConsole._on_fsm_state` · `SafetyConsole._on_start_process` · `SafetyConsole._reset_work` 외 1 | — |
| `SafetyConsole._step_name` | 1069 | 단계 번호로 레시피의 단계 이름을 찾는다. | 직접 `SafetyConsole._commit_button` · `SafetyConsole._publish_state` | — |
| `SafetyConsole._step_button` | 1075 | 단계 번호로 레시피의 버튼 이름을 찾는다. | 직접 `SafetyConsole._publish_state` | — |
| `SafetyConsole._begin_sub` | 1081 | 대기·공구 서브 작업을 시작하고 필요하면 공구 검사를 켠다. | 직접 `SafetyConsole._press_button` | — |
| `SafetyConsole._end_tool_scan` | 1095 | 공구 검사를 끄고 공구 판정 상태를 버린다. | 직접 `SafetyConsole._finish_sub` · `SafetyConsole._on_fsm_state` · `SafetyConsole._reset_work` | — |
| `SafetyConsole._tick_sub` | 1105 | 0.2초마다 서브 작업 시간을 흘리고 화면을 갱신한다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._update_sub_view` | 1111 | 서브 작업 상태에 맞춰 게이지·공구 경고를 보이고 조건이 차면 진행한다. | 직접 `SafetyConsole._begin_sub` · `SafetyConsole._on_tool` · `SafetyConsole._sim_tool_grasped` 외 1 | — |
| `SafetyConsole._finish_sub` | 1144 | 서브 작업을 끝내고 미뤄 둔 버튼 눌림을 FSM에 전한다. | 직접 `SafetyConsole._update_sub_view` | — |
| `SafetyConsole._on_cta` | 1161 | 가운데 「작업 시작」 버튼을 누르면 작업을 시작한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._on_alert_release` | 1170 | 경고·차단 배너의 해제 버튼을 처리한다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._release_block` | 1177 | 비상정지가 복귀됐을 때만 차단을 푼다. | 직접 `SafetyConsole._on_alert_release` | — |
| `SafetyConsole._on_reset_clicked` | 1199 | 비상정지 복귀와 사용자 확인 뒤 작업을 처음으로 되돌린다 — 비상정지는 확인창 앞뒤로 두 번 본다. (종합 리뷰 중요 1) | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._refuse_reset_if_emo` | 1362 | 비상정지가 복귀되지 않았으면 초기화를 거부하고 거부 창을 띄운다. (종합 리뷰 중요 1) | 직접 `SafetyConsole._on_reset_clicked` | — |
| `SafetyConsole._reset_work` | 1235 | 작업 상태·서브 작업·경고를 모두 지워 시작 전으로 돌린다. | 직접 `SafetyConsole._on_reset_clicked` | — |
| `SafetyConsole.keyPressEvent` ⚠️ | 1257 | 키보드 1~4·E·T·ESC로 버튼 눌림·공구·창 닫기를 흉내 낸다. | Qt | — |
| `SafetyConsole._on_fsm_state` | 1300 | FSM 상태가 바뀌면 상태판·경고·차단 표시와 집계를 맞춘다. | 연결 `SafetyConsole.__init__` | `FSM_EMO_BUTTON` |
| `SafetyConsole._on_interlock` | 1353 | 차단 신호를 기록하고 인터락 장치에 차단/복구를 보낸다. | 연결 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_feedback` | 1361 | 경고·차단 피드백을 로그하고 경고등 명령을 보낸다. | 연결 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_interlock_fault` | 1370 | 물리 차단이 확인 안 되면 경고창으로 즉시 알린다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._on_shutdown_clicked` | 1386 | 확인 뒤 녹화·장치를 정리하고 라즈베리파이를 끈다. | 직접 `SafetyConsole._init_ui` | — |
| `SafetyConsole._start_demo_capture` | 1424 | 시연영상 촬영(화면·1인칭 등)을 한 번만 시작한다. | 직접 `SafetyConsole._note_frame` / 연결 `SafetyConsole.__init__` | — |
| `SafetyConsole._stop_demo_capture` | 1451 | 시연영상 촬영을 멈추고 촬영 정보를 저장한다. | 직접 `SafetyConsole.closeEvent` / 연결 `SafetyConsole._on_shutdown_clicked` | — |
| `SafetyConsole._start_recording` ⚠️ | 1465 | 전체 화면 또는 카메라 영역 녹화를 시작한다. | 직접 `SafetyConsole._init_ui` / 연결 `SafetyConsole.__init__` | `RECORDING_CODEC` `RECORDING_FPS` `RECORDING_SAVE_DIR` |
| `SafetyConsole._capture_window_frame` | 1507 | 전체 화면 녹화 때 창 모습을 한 장씩 찍어 저장한다. | 직접 `SafetyConsole.__init__` | — |
| `SafetyConsole._record_camera_frame` | 1528 | 카메라 영역 녹화 때 받은 프레임을 영상에 적는다. | 직접 `SafetyConsole._note_frame` | — |
| `SafetyConsole._stop_recording` | 1542 | 녹화를 멈추고 파일 크기를 로그·알림으로 알린다. | 직접 `SafetyConsole._init_ui` · `SafetyConsole.closeEvent` / 연결 `SafetyConsole._on_shutdown_clicked` | — |
| `SafetyConsole.closeEvent` | 1559 | 창을 닫을 때 촬영·녹화·카메라·장치를 정리한다. | Qt | — |
| `SafetyConsole._on_stream_reset` | 862 | 카메라 재연결 신호를 받아 판정기의 손 관측을 지운다. (③ R5) | 연결 `camera_thread.CameraThread.stream_reset_signal` | — |
| `SafetyConsole._emo_button` | 1136 | 레시피의 비상정지 버튼 이름을 돌려준다(없으면 config 값). (② G3·G5) | 직접 `SafetyConsole._press_button` · `SafetyConsole._commit_button` · `SafetyConsole._on_fsm_state` | `FSM_EMO_BUTTON` |
| `SafetyConsole._show_block_banner` | 1171 | 차단 배너 문구를 고른다 — EMO 문구 · 인터락 미연결이면 「화면에서만 차단 중」. (③ I1) | 직접 `SafetyConsole._commit_button` · `SafetyConsole._on_fsm_state` | — |
| `SafetyConsole._cancel_sub` | 1184 | 진행 중인 서브 작업을 취소한다(타이머·공구 판정 종료 · 눌림 기록 버림). (② G1) | 직접 `SafetyConsole._on_fsm_state` | — |

### `overlay.py` (31개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `place` | 45 | 오버레이 위젯을 부모 영역 안에 비율(%)로 계산해 배치한다. | 직접 `AlertBanner.relayout` · `ConnBar.relayout` · `GaugePanel.relayout` 외 8 | — |
| `_glow` ⚠️ | 74 | 글자 후광을 넣던 자리지만 지금은 아무 일도 하지 않는다. | 직접 `AlertBanner._paint` · `_Panel.apply_theme` · `overlay_menu._Sheet.apply_theme` | — |
| `_Panel.__init__` | 95 | 반투명 패널의 공통 뼈대를 만들고 마우스 클릭을 통과시킨다. | 이름 후보 `camera_thread.CameraThread.__init__` · `AlertBanner.__init__` 외 13 | — |
| `_Panel.apply_theme` ⚠️ | 101 | 현재 테마 색으로 패널 배경(켜짐/꺼짐)을 다시 칠한다. | 이름 후보 `ConnBar.apply_theme` · `overlay_menu.CheckPanel.apply_theme` 외 6 | — |
| `StatusPanel.__init__` | 147 | 좌상단에 작업 상태와 공정 단계 목록 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `StatusPanel.update_view` ⚠️ | 177 | 현재 상태와 단계별 완료·진행·예정 표시를 화면에 갱신한다. | 직접 `StatusPanel.__init__` | — |
| `StatusPanel.apply_theme` | 238 | 테마가 바뀌면 판과 마지막으로 그린 단계 행을 새 색으로 다시 그린다. (④ U14) | 직접 `safety_console.SafetyConsole._apply_theme` | — |
| `StatusPanel.relayout` | 236 | 상태·단계 패널을 화면 왼쪽 위 정해진 비율 위치에 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `GaugePanel.__init__` | 247 | 상단 중앙에 대기 시간 게이지와 공구 안내 칸을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `GaugePanel.update_view` | 311 | 서브 작업 진행률과 필요한 공구·쥔 상태를 게이지에 보여 준다. | 이름 후보 `safety_console.SafetyConsole._finish_sub` · `safety_console.SafetyConsole._on_fsm_state` 외 2 | — |
| `GaugePanel.update_view._step` | 333 | 게이지 채움 막대를 목표값까지 조금씩 늘려 그린다. | 연결 `GaugePanel.update_view` | — |
| `GaugePanel._stop_gauge_anim` | 427 | 게이지 채움 보간이 살아 있으면 멈춘다 — 새 보간을 걸기 전과 숨길 때. (종합 리뷰 중요 2) | 직접 `GaugePanel.update_view` | — |
| `GaugePanel._stop_pulse` | 401 | 공구 안내 문구의 깜빡임을 멈추고 직전 공구 상태를 지운다. | 직접 `GaugePanel.update_view` | — |
| `GaugePanel.relayout` | 411 | 공구가 필요하면 넓게, 아니면 좁게 게이지 위치를 잡는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `GlowFrame.__init__` | 437 | 영상 테두리를 빛나게 할 투명 틀을 숨긴 채 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | — |
| `GlowFrame.set_level` | 443 | 경고·차단 단계에 맞춰 영상 테두리 색을 켜거나 끈다. | 직접 `safety_console.SafetyConsole._on_fsm_state` · `safety_console.SafetyConsole._reset_work` 외 1 | — |
| `GlowFrame.level` ⚠️ | 459 | 지금 켜진 테두리 단계(없음·경고·차단)를 알려 준다. | 연결 `GlowFrame.set_level` | — |
| `GlowFrame.apply_theme` | 490 | 테마가 바뀌면 지금 단계의 테두리를 새 색으로 다시 그린다. (④ U14) | 직접 `safety_console.SafetyConsole._apply_theme` | — |
| `GlowFrame.relayout` ⚠️ | 462 | 테두리를 화면 가운데 영상 영역(75%)에 맞춰 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `AlertBanner.__init__` | 480 | 경고·차단 안내 배너와 해제 버튼을 숨긴 채 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `AlertBanner.show_order_violation` | 524 | 순서 위반 경고 배너를 해제 버튼과 함께 띄운다. | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |
| `AlertBanner.show_wrong_tool` | 532 | 다른 공구를 쥐었다는 경고를 해제 버튼 없이 띄운다. | 직접 `safety_console.SafetyConsole._update_sub_view` | — |
| `AlertBanner.show_block` ⚠️ | 539 | 인터락 차단 배너를 차단 해제 버튼과 함께 띄운다. | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |
| `AlertBanner._paint` | 545 | 배너의 아이콘·제목·안내문·버튼·색을 한꺼번에 그린다. | 직접 `AlertBanner.show_block` · `AlertBanner.show_order_violation` · `AlertBanner.show_wrong_tool` | — |
| `AlertBanner.mode` | 588 | 지금 떠 있는 배너 종류(순서·공구·차단)를 알려 준다. | 직접 `safety_console.SafetyConsole._dim_others` · `safety_console.SafetyConsole._on_alert_release` 외 2 | — |
| `AlertBanner.hide_all` | 591 | 배너를 숨기고 깜빡임과 등장 예약을 모두 끈다. | 직접 `safety_console.SafetyConsole._on_fsm_state` · `safety_console.SafetyConsole._reset_work` 외 1 | — |
| `AlertBanner.relayout` | 598 | 차단이면 화면 중앙, 경고면 아래쪽에 배너를 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `ConnBar.__init__` | 626 | 우하단에 카메라·인터락·GPIO 연결 표시 아이콘을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `ConnBar.update_state` | 644 | 연결 상태에 따라 아이콘 배경을 초록·빨강·회색으로 칠한다. | 직접 `ConnBar.__init__` · `ConnBar.apply_theme` / 직접 `safety_console.SafetyConsole._update_conn_bar` | — |
| `ConnBar.apply_theme` | 656 | 테마가 바뀌면 연결 표시 색을 다시 칠한다. | 이름 후보 `overlay_menu.CheckPanel.apply_theme` · `overlay_menu.MenuPanel.apply_theme` 외 5 | — |
| `ConnBar.relayout` ⚠️ | 660 | 연결 표시줄을 오른쪽 아래 구석에 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `AlertBanner.apply_theme` | 615 | 경고·차단 배너의 판·테두리를 배경 설정과 무관하게 늘 그린다. (② G8) | 이름 후보 `safety_console.SafetyConsole._apply_theme` | — |

### `overlay_menu.py` (34개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_Sheet.__init__` | 59 | 열었을 때만 나오는 진한 패널을 숨긴 채 만든다. | 이름 후보 `camera_thread.CameraThread.__init__` · `overlay.AlertBanner.__init__` 외 13 | — |
| `_Sheet._make_close_button` | 63 | 패널 오른쪽 위에 닫기(✕) 버튼을 만들어 준다. | 직접 `CheckPanel.__init__` · `RecordPanel.__init__` · `SettingsPanel.__init__` | `font` |
| `_Sheet.apply_theme` ⚠️ | 84 | 시트 패널에 진한 배경과 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `CheckPanel.apply_theme` 외 6 | — |
| `_row_button` | 93 | 메뉴 항목용 글자형 버튼을 만든다. | 직접 `MenuPanel.__init__` | `font` |
| `NotifyButton.__init__` ⚠️ | 115 | 알림(🔔) 버튼과 숫자 배지를 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `NotifyButton.set_count` | 123 | 안 읽은 알림 수를 배지에 표시하고 0이면 숨긴다. | 직접 `safety_console.SafetyConsole._notify` · `safety_console.SafetyConsole._toggle_notify` | — |
| `NotifyButton._place_badge` | 133 | 배지를 버튼 안 오른쪽 위 모서리에 맞춰 놓는다. | 직접 `NotifyButton.resizeEvent` · `NotifyButton.set_count` | — |
| `NotifyButton.resizeEvent` | 146 | 버튼 크기가 바뀌면 배지 위치를 다시 잡는다. | 이름 후보 `safety_console.SafetyConsole.resizeEvent` | — |
| `MenuPanel.__init__` ⚠️ | 167 | 점검·녹화·로그·설정·초기화·종료 메뉴 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `MenuPanel._hline` | 215 | 메뉴 구분선 한 줄을 만든다. | 직접 `MenuPanel.__init__` | — |
| `MenuPanel.apply_theme` ⚠️ | 221 | 메뉴 제목·항목·종료 버튼·구분선에 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `CheckPanel.apply_theme` 외 5 | — |
| `MenuPanel.relayout` | 241 | 메뉴 패널을 ☰ 버튼 아래 오른쪽에 세로로 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `NotifyPanel.__init__` ⚠️ | 259 | 알림 목록을 담을 스크롤 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `NotifyPanel.push` ⚠️ | 287 | 알림 한 건을 아이콘과 함께 목록 맨 아래에 추가한다. | 직접 `safety_console.SafetyConsole._notify` | `font` |
| `NotifyPanel._scroll_to_latest` | 342 | 알림 목록을 가장 최근 항목이 보이게 내린다. | 직접 `NotifyPanel.push` · `NotifyPanel.showEvent` | — |
| `NotifyPanel._paint` | 343 | 알림 글자에 색을 칠하고 기억한다 — 테마가 바뀌면 다시 칠한다. (④ U14) | 직접 `NotifyPanel.push` | — |
| `NotifyPanel.showEvent` | 346 | 알림 패널을 열 때 최신 알림 위치로 스크롤한다. | Qt | — |
| `NotifyPanel.count` ⚠️ | 351 | 쌓인 알림 개수를 알려 준다. | 직접 `fsm.SafetyFSM.update_vision` · `overlay_result.ResultPanel.show_result` 외 1 | — |
| `NotifyPanel.apply_theme` | 354 | 알림 패널 제목에 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `CheckPanel.apply_theme` 외 5 | — |
| `NotifyPanel.relayout` | 358 | 알림 패널을 🔔 버튼 위 왼쪽에 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `SettingsPanel.__init__` ⚠️ | 379 | 공구·테마·패널배경·탐지박스를 고르는 설정 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `SHOW_DETECT_BOXES` `font` |
| `SettingsPanel.set_tools` | 461 | 레시피가 준 공구 목록으로 선택 버튼들을 다시 만든다. | 직접 `safety_console.SafetyConsole._wire_tool_settings` | `font` |
| `SettingsPanel.set_tool_editable` | 479 | 작업 중에는 공구를 못 바꾸게 막고 안내문을 띄운다. | 직접 `safety_console.SafetyConsole._toggle_settings` | — |
| `SettingsPanel.apply_theme` | 490 | 설정 패널의 제목·라디오 버튼에 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `CheckPanel.apply_theme` 외 5 | — |
| `SettingsPanel.relayout` | 513 | 설정 패널을 화면 한가운데 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `CheckPanel.__init__` | 527 | 연결 점검 결과와 '다시 점검' 버튼이 있는 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `CheckPanel.update_results` | 554 | 점검 항목별 ✓/✗와 설명, 필요 시 재연결 버튼을 그린다. | 직접 `safety_console.SafetyConsole._run_manual_check` | `font` |
| `CheckPanel.apply_theme` | 604 | 점검 패널 제목과 버튼에 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `MenuPanel.apply_theme` 외 5 | — |
| `CheckPanel.relayout` ⚠️ | 612 | 점검 패널을 화면 한가운데 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |
| `RecordPanel.__init__` | 631 | 전체화면/카메라 녹화 시작·중지 버튼이 있는 패널을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `RecordPanel.set_save_dir` ⚠️ | 687 | 녹화 파일 저장 폴더 경로를 버튼에 표시한다. | 직접 `safety_console.SafetyConsole._init_ui` | — |
| `RecordPanel.set_state` ⚠️ | 692 | 녹화 중/중지 상태에 맞춰 문구와 버튼을 바꾼다. | 직접 `RecordPanel.__init__` · `RecordPanel.apply_theme` / 직접 `safety_console.SafetyConsole._open_record` · `safety_console.SafetyConsole._start_recording` 외 1 | — |
| `RecordPanel.apply_theme` ⚠️ | 707 | 녹화 패널 버튼·글자에 테마 색을 입힌다. | 이름 후보 `overlay.ConnBar.apply_theme` · `CheckPanel.apply_theme` 외 5 | — |
| `RecordPanel.relayout` | 724 | 녹화 패널을 화면 한가운데 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |

### `overlay_result.py` (9개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_hhmmss` | 24 | 초를 「N분 M초」 같은 읽기 쉬운 글로 바꾼다. | 직접 `ResultPanel.show_result` | — |
| `_stamp` | 29 | 시각 값을 날짜·시각 글자로 바꾼다. | 직접 `ResultPanel.show_result` | — |
| `ResultPanel.__init__` | 40 | 작업 완료 결과창의 제목·판정·본문·주의문 칸을 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | `font` |
| `ResultPanel.show_result` | 77 | 작업 결과(순서·시간·위반·차단·공구·검출)를 결과창에 채워 띄운다. | 직접 `safety_console.SafetyConsole._show_result` | — |
| `ResultPanel.show_result.tname` | 108 | 공구 키를 화면용 이름(예: 렌치)으로 바꾼다. | 직접 `ResultPanel.show_result` | — |
| `ResultPanel._det_line` | 143 | 검출 항목 하나를 「검출 N 프레임·평균 신뢰도」 한 줄로 만든다. | 직접 `ResultPanel.show_result` | — |
| `ResultPanel._section` | 152 | 결과창에 제목 하나와 그 아래 여러 줄을 붙인다. | 직접 `ResultPanel.show_result` | `font` |
| `ResultPanel.apply_theme` ⚠️ | 164 | 결과창 제목·주의문 등에 현재 테마 색을 입힌다. | 직접 `ResultPanel.show_result` | — |
| `ResultPanel.relayout` | 171 | 결과창을 화면 가운데 정해진 비율 크기로 놓는다. | 이름 후보 `safety_console.SafetyConsole._relayout` · `safety_console.SafetyConsole._show_result` 외 2 | — |

### `anim.py` (17개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `enabled` | 34 | 화면 애니메이션을 켤지 설정값(UI_ANIMATION)을 확인한다. | 직접 `Pulse.start` · `TextPulse.start` · `flash` 외 2 | — |
| `tween` | 38 | 0에서 1로 부드럽게 변하는 값을 만들어 애니메이션을 돌린다. | 직접 `Pulse.start` · `TextPulse.start` · `flash` 외 3 | — |
| `flash` | 58 | 표시 줄을 잠깐 색으로 반짝였다가 원래 모양으로 되돌린다. | 직접 `overlay.GaugePanel.update_view` · `overlay.StatusPanel.update_view` | — |
| `flash.step` | 64 | 반짝임 배경색을 점점 옅게 바꿔 칠한다. | 연결 `Pulse.start` · `TextPulse.start` 외 1 | — |
| `slide_in` | 74 | 안내 배너를 아래에서 미끄러지듯 제자리로 올린다. | 직접 `overlay.AlertBanner.relayout` | — |
| `busy` | 88 | 위젯이 지금 움직이는 중인지 알려 준다. | 직접 `overlay.AlertBanner.relayout` | — |
| `Pulse.__init__` | 100 | 차단 배너 테두리 맥박 효과를 준비한다. | 직접 `overlay.AlertBanner.__init__` | — |
| `Pulse.start` | 106 | 차단 배너 테두리가 천천히 깜빡이기 시작한다. | 이름 후보 `tween` · `camera_thread.CameraThread.run` 외 13 | — |
| `Pulse.start.step` | 112 | 테두리 색의 진하기를 단계에 맞춰 바꾼다. | 연결 `Pulse.start` · `TextPulse.start` 외 1 | — |
| `Pulse.stop` ⚠️ | 123 | 테두리 깜빡임을 멈춘다. | 이름 후보 `TextPulse.stop` · `camera_thread.CameraThread.set_tool_scan` 외 12 | — |
| `Pulse.active` | 128 | 테두리 깜빡임이 돌고 있는지 알려 준다. | **없음** | — |
| `TextPulse.__init__` | 140 | 공구 안내 글자의 밝기 맥박 효과를 준비한다. | 직접 `overlay.GaugePanel.__init__` | — |
| `TextPulse._apply` | 148 | 글자색을 주어진 진하기로 칠한다. | 직접 `TextPulse.start` · `TextPulse.start.step` · `TextPulse.stop` | — |
| `TextPulse.start` | 154 | 공구 안내 글자가 밝아졌다 어두워지기를 시작한다. | 이름 후보 `tween` · `camera_thread.CameraThread.run` 외 13 | — |
| `TextPulse.start.step` | 162 | 한 바퀴 동안 글자 진하기를 올렸다 내린다. | 연결 `Pulse.start` · `TextPulse.start` 외 1 | — |
| `TextPulse.stop` | 170 | 글자 맥박을 멈추고 또렷한 상태로 둔다. | 이름 후보 `Pulse.stop` · `camera_thread.CameraThread.set_tool_scan` 외 12 | — |
| `TextPulse.active` | 176 | 글자 맥박이 돌고 있는지 알려 준다. | **없음** | — |

### `theme.py` (6개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `set_theme` | 107 | 화면 테마(다크/화이트)를 바꾼다. | 직접 `safety_console.SafetyConsole._on_theme_changed` | — |
| `current` | 115 | 지금 쓰는 테마 이름을 알려 준다. | 직접 `overlay_menu.SettingsPanel.__init__` · `safety_console.SafetyConsole._apply_theme` | — |
| `C` | 119 | 현재 테마에서 이름에 맞는 색 값을 꺼내 준다. | 직접 `overlay.AlertBanner.__init__` · `overlay.AlertBanner._paint` · `overlay.ConnBar.update_state` 외 16 | — |
| `panel_qss` | 124 | 오버레이 패널의 배경·테두리·글자 스타일을 만든다. | 직접 `overlay.AlertBanner.__init__` · `overlay.AlertBanner._paint` · `overlay._Panel.apply_theme` 외 2 | — |
| `text_qss` ⚠️ | 138 | 글자의 색·굵기 스타일을 만든다. | 직접 `overlay.AlertBanner._paint` · `overlay.GaugePanel.update_view` · `overlay.StatusPanel.update_view` 외 13 | — |
| `gauge_qss` | 144 | 게이지 막대 바탕의 스타일을 만든다. | 직접 `overlay.GaugePanel.update_view` | — |

### `precheck.py` (7개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `CheckResult.__init__` | 23 | 점검 한 항목의 이름·통과 여부·설명을 담는다. | 직접 `run_stage1` · `run_stage2` | — |
| `CheckResult.mark` | 31 | 통과면 ✓, 실패면 ✗ 기호를 돌려준다. | 직접 `overlay_menu.CheckPanel.update_results` | — |
| `CheckResult.__repr__` | 34 | 점검 결과를 디버그용 짧은 글로 보여 준다. | 객체 생성·파이썬 | — |
| `run_stage1` ⚠️ | 43 | 카메라·모델·인터락·GPIO가 연결·로드됐는지 점검한다. | 직접 `safety_console.SafetyConsole._run_manual_check` · `safety_console.SafetyConsole._update_conn_bar` | — |
| `run_stage2` ⚠️ | 82 | 작업 시작 전 영상·버튼·손 검출·인터락이 실제로 되는지 본다. | **없음** | — |
| `_probe_hand` ⚠️ | 122 | 프레임 한 장으로 손 검출 추론이 오류 없이 도는지 시험한다. | 직접 `run_stage2` | — |
| `summary` ⚠️ | 137 | 점검 결과 묶음이 전부 통과인지와 '3/5' 같은 통과 수를 돌려준다. | **없음** | — |

### `session_stats.py` (14개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_now` | 19 | 시각을 받으면 그대로, 없으면 현재 시각을 쓴다. | 직접 `SessionStats.button_pressed` · `SessionStats.finish` · `SessionStats.interlock` 외 5 | — |
| `SessionStats.__init__` | 24 | 작업 결과 집계기를 빈 상태로 만든다. | 직접 `safety_console.SafetyConsole._init_ui` | — |
| `SessionStats.reset` | 27 | 모아 둔 단계·위반·공구·검출 기록을 모두 비운다. | 직접 `SessionStats.__init__` · `SessionStats.start` | — |
| `SessionStats.start` | 41 | 새 작업 집계를 시작하고 레시피 이름과 시작 시각을 적는다. | 이름 후보 `anim.tween` · `camera_thread.CameraThread.run` 외 13 | — |
| `SessionStats.running` | 48 | 지금 작업 집계가 진행 중인지 알려 준다. | 직접 `safety_console.SafetyConsole._on_yolo_detections` | — |
| `SessionStats.button_pressed` ⚠️ | 52 | 올바른 버튼이 눌린 시각을 단계 소요시간용으로 적어 둔다. | 직접 `safety_console.SafetyConsole._press_button` | — |
| `SessionStats.step_done` | 56 | 한 단계가 끝난 시각과 걸린 시간을 기록한다. | 직접 `safety_console.SafetyConsole._commit_button` | — |
| `SessionStats.sub_started` | 64 | 공구가 필요한 서브 작업이 시작되면 공구 기록 칸을 만든다. | 직접 `safety_console.SafetyConsole._begin_sub` | — |
| `SessionStats.sub_done` ⚠️ | 79 | 서브 작업 종료 알림을 받지만 아무것도 하지 않는다. | 직접 `safety_console.SafetyConsole._finish_sub` | — |
| `SessionStats.tool_grasped` | 82 | 요구 공구를 쥔 시간과 다른 공구를 쥔 횟수를 센다. | 직접 `safety_console.SafetyConsole._on_tool` · `safety_console.SafetyConsole._sim_tool_grasped` 외 1 | — |
| `SessionStats.violation` | 95 | 순서 위반 한 건(기대·실제 버튼, 경고/차단)을 기록한다. | 직접 `safety_console.SafetyConsole._on_fsm_state` | — |
| `SessionStats.interlock` | 99 | 인터락이 걸린 시각과 풀린 시각을 기록한다. | 직접 `safety_console.SafetyConsole._on_interlock` | — |
| `SessionStats.frame` ⚠️ | 107 | 프레임 한 장의 검출 이름별 등장 수와 신뢰도 합을 더한다. | 이름 후보 `safety_console.SafetyConsole._on_yolo_detections` · `voice_assistant.run` | — |
| `SessionStats.finish` ⚠️ | 125 | 작업 결과를 한 묶음으로 정리해 돌려주고 집계를 멈춘다. | 직접 `safety_console.SafetyConsole._show_result` | — |

### `fps.py` (2개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `fps_from_intervals` | 15 | 프레임 도착 간격으로 실제 초당 프레임 수를 낸다. | 직접 `safety_console.SafetyConsole._update_conn_bar` | — |
| `fps_stale` ⚠️ | 29 | 최근 프레임이 끊겼는지 알려준다. | 직접 `safety_console.SafetyConsole._note_frame` · `safety_console.SafetyConsole._update_conn_bar` | — |

### `state_publisher.py` (3개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `StatePublisher.__init__` | 25 | 음성비서에 줄 상태 파일 위치를 정하고 폴더를 만든다. | 직접 `safety_console.SafetyConsole.__init__` | `STATE_SHM_DIR` |
| `StatePublisher.publish` | 38 | 현재 작업 상태를 공유 메모리 파일에 안전하게 통째로 쓴다. | 직접 `safety_console.SafetyConsole._publish_state` | — |
| `StatePublisher.clear` ⚠️ | 60 | 작업이 없을 때 상태 파일을 지운다. | 직접 `camera_thread.CameraThread.run` · `fsm.SafetyFSM.update_vision` 외 9 | — |

### `demo_recorder.py` (10개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `keep_screen_awake` ⚠️ | 25 | 촬영 중 화면보호기가 켜져 화면이 꺼지지 않게 막는다. | 직접 `DemoRecorder._feed` | — |
| `DemoRecorder.__init__` ⚠️ | 44 | 시연영상 촬영 한 세트의 저장 위치·이름·상태값을 준비한다. | 직접 `safety_console.SafetyConsole.__init__` | — |
| `DemoRecorder.path_for` | 58 | 촬영 영상 파일이 저장될 경로를 만든다. | 직접 `DemoRecorder.start` | — |
| `DemoRecorder.submit_camera` | 78 | 카메라가 넘긴 최신 프레임 두 장을 보관만 해 둔다. | 연결 `safety_console.SafetyConsole.__init__`(② G10 — 녹화 시작 전부터 붙여 둔다) | — |
| `DemoRecorder.start` | 71 | 영상 녹화를 시작하고 촬영 정보 파일을 남긴다. | 이름 후보 `anim.tween` · `camera_thread.CameraThread.run` 외 12 | `DEMO_CAPTURE_SIZE` `DEMO_FPV_SIZE` `DEMO_HIDE_VIDEO` `demo_wants` |
| `DemoRecorder._feed` ⚠️ | 103 | 1초에 15번씩 최신 1인칭 프레임을 녹화 프로세스에 넘긴다. | 연결 `DemoRecorder.start` | `DEMO_CAPTURE_FPS` |
| `DemoRecorder.stop` | 132 | 녹화를 멈추고 촬영 시간·넘긴 프레임 수를 돌려준다. | 이름 후보 `anim.Pulse.stop` · `anim.TextPulse.stop` 외 12 | `DEMO_CAPTURE_FPS` |
| `DemoRecorder.write_info` | 142 | 촬영 설정·길이·영상 파일 크기를 촬영정보.txt 에 적는다. | 직접 `safety_console.SafetyConsole._stop_demo_capture` | `DEMO_CAPTURE_FPS` `DEMO_TARGETS` |
| `DemoRecorder._mark_dead` | 61 | 멈춘 1인칭 녹화 이름을 적어 둔다. (② G12) | 직접 `DemoRecorder._feed` | — |
| `DemoRecorder.take_dead` | 65 | 멈춘 1인칭 녹화 이름을 가져가고 비운다(GUI 알림용). (② G12) | 직접 `safety_console.SafetyConsole._update_conn_bar` | — |

### `demo_ffmpeg.py` (5개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_screen_size` | 33 | 현재 모니터 화면의 가로·세로 크기를 알아낸다. | 직접 `FfmpegSet.start` | — |
| `FfmpegSet.__init__` | 53 | 촬영용 ffmpeg 프로세스 목록과 1인칭 파이프 목록을 빈 채로 준비한다. | 직접 `demo_recorder.DemoRecorder.__init__` | — |
| `FfmpegSet._spawn` | 57 | 주어진 옵션으로 ffmpeg 프로세스 하나를 띄운다. | 직접 `FfmpegSet.start` | — |
| `FfmpegSet.start` ⚠️ | 62 | GUI 화면 녹화와 1인칭 영상 두 벌 녹화를 시작하고 문제를 알려준다. | 이름 후보 `anim.tween` · `camera_thread.CameraThread.run` 외 13 | `DEMO_CAPTURE_FPS` `demo_wants` |
| `FfmpegSet.stop` | 111 | 녹화 프로세스들을 안전하게 끝내 영상 파일을 마무리한다. | 이름 후보 `anim.Pulse.stop` · `anim.TextPulse.stop` 외 13 | — |

### `voice_assistant.py` (26개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `log` | 74 | 현재 시각을 붙여 메시지를 화면에 출력한다. | 직접 `Speaker._ensure` · `Speaker.play` · `Speaker.send` 외 2 | — |
| `metric` | 78 | 발화마다 측정값 한 줄을 기록 파일에 덧붙인다. | 직접 `run` | — |
| `AudioLog.__init__` | 96 | 오디오 기록 폴더를 만들고 마이크 전체 녹음 파일을 연다. | 직접 `run` | — |
| `AudioLog.mic` | 109 | 마이크로 받은 소리를 전체 녹음 파일에 이어 쓴다. | 연결 `run` | — |
| `AudioLog.utterance` | 113 | 잘라낸 말소리와 그 인식 글자를 짝지어 저장한다. | 직접 `run` | — |
| `AudioLog.played` | 127 | 스피커로 튼 미리 만든 음성 파일을 복사해 남긴다. | 직접 `Speaker.play` | — |
| `AudioLog.played_pcm` | 137 | 그때 합성해 튼 음성과 그 문장을 파일로 남긴다. | 직접 `run` | — |
| `AudioLog.close` ⚠️ | 159 | 마이크 전체 녹음 파일을 닫는다. | 이름 후보 `camera_thread.CameraThread.run` · `camera_thread.CameraThread.stop` 외 12 | — |
| `esp_ip` ⚠️ | 165 | 오디오 보드(ESP32)의 IP 주소를 파일에서 읽어 온다. | 직접 `main` / 연결 `run` | — |
| `build_stt` | 181 | 한국어 음성인식(STT) 모델을 불러와 준비한다. | 직접 `run` | — |
| `transcribe` | 201 | 잘라낸 말소리를 글자로 바꾼다. | 직접 `run` | — |
| `wav_payload` ⚠️ | 208 | 음성 파일을 ESP32 스피커로 보낼 전송 묶음으로 만든다. | 직접 `Speaker.play` | — |
| `connect_mic` | 220 | ESP32 마이크 연결이 될 때까지 계속 다시 붙는다. | 직접 `run` | — |
| `Speaker.__init__` ⚠️ | 250 | ESP32 스피커 명령 채널의 주소를 기억해 둔다. | 직접 `run` | — |
| `Speaker._ensure` | 255 | 스피커 채널이 없으면 연결하고 음량을 5단계로 맞춘다. | 직접 `Speaker.send` | — |
| `Speaker._drain` | 268 | ESP32 가 보낸 재생 결과 응답을 읽어 모은다. | 직접 `Speaker.send` | — |
| `Speaker.send` | 301 | 스피커 채널로 데이터를 보내고 실패하면 한 번 더 시도한다. | 직접 `Speaker.chime` · `Speaker.play` / 직접 `run` | — |
| `Speaker.reset` | 319 | 끊긴 스피커 연결을 버려 다음에 새로 붙게 한다. | 이름 후보 `safety_console.SafetyConsole._reset_work` · `run` | — |
| `Speaker.chime` | 328 | ESP32 에 「띠링」 소리를 내라고 명령한다. | 직접 `run` | — |
| `Speaker.play` | 331 | 미리 만든 답변 음성을 ESP32 로 보내 틀고 성공 여부를 확인한다. | 직접 `run` | — |
| `ask_async` | 345 | LLM 질문을 뒤에서 따로 돌려 기다리는 시간을 줄인다. | 직접 `run` | — |
| `ask_async._work` | 354 | LLM 에 물어 받은 답을 상자에 담는다. | 연결 `ask_async` | — |
| `run` ⚠️ | 362 | 마이크 소리를 계속 듣다가 호출어·질문을 알아듣고 음성으로 답한다. | 직접 `main` | `LLM_ENABLED` `LLM_TIMEOUT_SEC` |
| `main` ⚠️ | 574 | 명령줄 옵션을 읽어 음성비서를 시작한다. | 직접 `voice_assistant(최상위)` | — |
| `open_audio_log` | 166 | 오디오 기록을 열고 프로그램이 끝날 때 닫도록 등록한다. (③ V4) | 직접 `run` | — |
| `Speaker._readline` | 314 | 명령 채널에서 한 줄을 소켓으로 직접 읽는다(타임아웃이면 None). (③ V1) | 직접 `Speaker._drain` | — |

### `voice_lib.py` (10개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_norm` | 45 | 글자에서 띄어쓰기와 문장부호를 지운다. | 직접 `is_question` · `is_tool_question` · `is_wake` | — |
| `is_wake` ⚠️ | 50 | 말 속에 호출어 「가디언」이 들어 있는지 판단한다. | 직접 `voice_assistant.run` · `is_question` | — |
| `is_tool_question` | 56 | 「앞에 보이는 게 뭐야」처럼 공구를 묻는 말인지 판단한다. | 직접 `voice_assistant.run` | — |
| `is_question` | 64 | 호출 뒤 대기 시간 안의 말을 비서에게 한 질문으로 본다. | 직접 `voice_assistant.run` | — |
| `rms` | 89 | 소리 조각의 크기(음량)를 계산한다. | 직접 `_frame_rms` | — |
| `_frame_rms` | 102 | 소리를 20ms 조각으로 나눠 조각마다 크기를 계산한다. | 직접 `find_utterance` · `noise_floor` | — |
| `noise_floor` | 119 | 주변 소음이 얼마나 큰지 계산한다. | 직접 `voice_assistant.run` · `find_utterance` | — |
| `find_utterance` | 135 | 녹음 속에서 사람이 말한 구간의 시작과 끝을 찾는다. | 직접 `voice_assistant.run` | — |
| `read_tool_dets` ⚠️ | 190 | 공구 인식 결과 파일을 읽고 최신인지 확인한다. | 직접 `voice_assistant.run` | — |
| `answer_key` | 212 | 공구 인식 결과에 맞는 답변 음성 파일 이름을 고른다. | 직접 `voice_assistant.run` | — |

### `voice_card.py` (7개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `_state_path` | 33 | GUI 상태 파일(state.json)의 위치를 정한다. | 직접 `read_state` | `STATE_SHM_DIR` |
| `read_state` | 40 | GUI 가 남긴 작업 상태를 읽고 믿을 수 없으면 버린다. | 직접 `voice_assistant.run` | — |
| `_seen_tool` ⚠️ | 66 | 카메라에 지금 가장 확실히 보이는 공구 이름을 고른다. | 직접 `build_card` · `card_facts` | — |
| `_state_bucket` | 80 | 작업 상태를 차단·경고·정상 셋 중 하나로 묶는다. | 직접 `card_facts` | — |
| `card_facts` | 94 | 답이 맞는지 대조할 공구·단계·버튼·상태 값을 모은다. | 직접 `voice_assistant.run` | — |
| `build_card` | 105 | LLM 에게 줄 현재 작업 사실을 글로 정리한다. | 직접 `voice_assistant.run` | — |
| `verify_answer` | 168 | LLM 답이 말하는 사이 상황이 바뀌어 틀리지 않았는지 확인한다. | 직접 `voice_assistant.run` | — |

### `voice_llm.py` (1개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `ask` | 45 | 작업 사실과 질문을 LLM(ollama)에 보내 답 문장을 받는다. | 직접 `voice_assistant.ask_async` · `voice_assistant.ask_async._work` | `LLM_MODEL` `LLM_NUM_CTX` `LLM_NUM_PREDICT` `LLM_TIMEOUT_SEC` 외 1 |

### `voice_tts.py` (3개)

| 함수 | 줄 | 하는 일 | 부르는 곳 | 쓰는 설정값 |
|---|---|---|---|---|
| `Tts.__init__` | 23 | 한국어 음성합성(TTS) 모델을 불러와 들고 있는다. | 직접 `voice_assistant.run` | — |
| `Tts.synth` | 35 | 문장을 사람 목소리 음성 데이터로 만든다. | 직접 `voice_assistant.run` | — |
| `frame` ⚠️ | 49 | 합성한 음성을 ESP32 스피커로 보낼 전송 묶음으로 만든다. | 직접 `voice_assistant.run` | — |

## 4. 파악하며 보인 것 (조치하지 않음)

> ✅ = 코드에서 직접 다시 확인함 · 나머지는 함수를 읽으며 적은 메모(4.2 표)이고 **재확인 전**이다. 고칠지·어떻게 고칠지는 정하지 않았다.

### 4.1 동작에 영향이 있을 수 있는 것

| # | 무엇 | 어디 | 확인 |
|---|---|---|---|
| 1 | ✅ **해결 — 인식·음성 수정 ③(2026-09-25)** · 손 검출을 박스를 그리기 전 프레임으로 · ~~**손 검출이 버튼 박스가 그려진 프레임을 입력으로 받는다** — 「표시만 바꾼다」는 설정(박스 표시)이 손 검출 입력을 바꾼다. 시연 촬영 중에는 박스가 늘 그려진다~~ | `camera_thread._process_frame`(박스 그림 → 손 검출) · `set_draw_boxes` 설명 | ✅ |
| 2 | ✅ **해결 — GUI 수정 ②(2026-09-25)** · ~~**EMO 로 차단돼도 배너가 「순서 위반이 계속되어 인터락이 작동했습니다」라고 쓴다** — 바로 위 주석은 「EMO 는 순서 위반이 아니다」~~ — EMO 차단은 「비상정지로 차단됐습니다」 | `safety_console._on_fsm_state` → `overlay.AlertBanner.show_block()`(인자 없음) | ✅ |
| 3 | **공구를 쥐었는지 판정하는 주 규칙이 지금 모델에서 동작하지 않는다** — `-in-hand` 클래스를 기대하는데(`tool_v4` 용 · 미채택) 쓰는 모델은 `tool_v3.pt`(driver·wrench·pliers). 손끝이 공구 박스 안에 드는 보조 규칙만 쥠을 판정한다 | `tool_state.ToolState._in_hand_tool` · `config.TOOL_MODEL_PATH` | ✅ |
| 4 | ✅ **해결 — GUI 수정 ②(2026-09-25)** · ~~**설정 창에 「4단계 지정 공구」가 박혀 있다**~~ — `recipe.json` 에서 공구 서브 작업은 **2단계(B2)** | `overlay_menu.SettingsPanel.__init__` | ✅ |
| 5 | ✅ **해결 — GUI 수정 ②(2026-09-25)** · ~~**음성비서에게 넘기는 공구가 설정에서 바꾼 공구를 반영하지 않는다**~~ — 레시피 원본을 읽는다(판정용 `_sub_spec_for` 는 반영한다) | `safety_console._publish_state` | ✅ |
| 6 | ~~**WARNING 중에 정답 버튼을 눌러도 무시된다**~~ — ✅ **해결 — 판정 수정 ①(2026-09-25)**: 경고 해제 + 단계 완료(규칙 변경 · 상위 §7.3-6) | `fsm.SafetyFSM.press_button` | ✅ 해결 |
| 7 | ✅ **해결 — 인식·음성 수정 ③(2026-09-25)** · 설명을 동작에 맞췄다(동작은 그대로) · ~~호출어 판정이 설명과 다르다 — 「앞 두 글자 접두 매칭」이라 적혀 있으나 실제는 **문장 어디든 포함**이면 호출로 본다~~ | `voice_lib.is_wake` | ✅ |
| 8 | ✅ **해결 — 인식·음성 수정 ③(2026-09-25)** · 종료 때 닫도록 등록(`open_audio_log`) · ~~음성 녹음 파일(`마이크_전체.wav`)을 닫는 함수를 아무도 부르지 않는다 — 파일이 제대로 마무리되지 않을 수 있다~~ | `voice_assistant.AudioLog.close` | ✅ |
| 9 | ~~체류 임계값이 두 곳에 있고 값이 없으면 GUI 가 오류~~ — ✅ **해결 — 판정 수정 ①(2026-09-25)**: 없으면 로더가 config 값을 채우고, 두 값이 같은지 `selftest/test_recipe.py` 가 검사한다 | `recipe._validate` | ✅ 해결 |
| 10 | ✅ **해결 — GUI 수정 ②(2026-09-25)** · ~~1인칭 녹화 둘 중 하나가 죽으면 나머지도 조용히 멈춘다~~ — 남은 쪽은 계속하고 알린다 | `demo_recorder._feed` · `demo_ffmpeg.FfmpegSet.start` | 메모 |

### 4.2 이 프로그램 안에서 아무도 부르지 않는 함수

| 함수 | 비고 | 확인 |
|---|---|---|
| `precheck.run_stage2`(+ `_probe_hand` · `summary`) | 「작업 시작 점검(2차)」— GUI 는 `run_stage1` 만 쓴다. 자가 테스트만 부른다 | ✅ |
| `CameraThread.set_active` · `set_host` | 웹캠 제거 전 카메라 전환용으로 보인다 · `set_active` 가 없으니 `_process_frame` 의 비활성 분기는 실행될 일이 없다 | ✅ |
| `hailo_device.shutdown` | 「평상시엔 부를 필요 없다」고 적혀 있다 | ✅ |
| `interlock.InterlockController.gave_up` | 점검은 카메라 쪽 `gave_up` 만 읽는다 | 메모 |
| `frame_orient._selftest` | 측정 도구 몇 곳이 부른다(런타임은 안 부름) | ✅ |
| `camera_thread.roi_at_point` · `CameraThread.draw_boxes` · `frame_orient.undistort_map` · `frame_orient.apply` | 자가 테스트·측정 도구 전용 호환 함수 | ✅ |
| `detector.PyTorchDetector` | 백엔드가 `hailo` 라 쓰이지 않는다 | 메모 |
| `anim.Pulse.active` · `TextPulse.active` | 자가 테스트 전용 | ✅ |

### 4.3 설정(config)을 따르지 않고 값을 따로 적은 곳 (메모)

- 버튼 클래스 이름표(`detector.HailoDetector`) · 손 모델 파일 이름(`hand_tracker`) · 수신 버퍼 1MB(`camera_thread`) · 끊김 임계 2.0초(`fps` 와 `precheck`) · `.camera_ip` 경로·공구 공유 폴더(음성비서) · 녹화 기본 크기 `(640, 480)`(회전 뒤는 세로 — `config.DEMO_FPV_SIZE` 와 다름) · 점검 문구의 「console_v2 로드됨」.


### 4.4 함수별 메모 (3절에서 ⚠️ 가 붙은 함수 — 코드를 읽으며 적은 것 · 재확인 전)

| 모듈 | 함수 | 줄 | 메모 |
|---|---|---|---|
| `main` | `main` | 11 | 주석이 서로 어긋난다 — 24-26행은 SOP_FULLSCREEN 촬영에 showMaximized 를 쓴다고, 28-29행은 「최대화·전체화면을 쓰지 않는다」고 적었고 52-53행은 여전히 showMaximized 한다(run_scenario.sh:87 이 SOP_FULLSCREEN=1 설정). 전역 스타일(15-22행)은 theme.py 가 아니라 config 단일 색(BG_PRIMARY 등)이라 화이트 테마로 바꿔도 안 바뀐다. |
| `config` | `demo_wants` | 251 | 웹캠 제거(2026-09-23) 뒤 허용 값이 'all'·'fpv+gui' 둘뿐이고 호출은 'fpv'·'gui' 뿐이라(demo_recorder:98·demo_ffmpeg:85,93) 항상 True — 사실상 판단하지 않는 잔재. |
| `config` | `_resolve_interlock_port` | 311 | ⚠️ 뒷 문장 낡음(③ I1) — 지금은 `resolve_interlock_port` 이고 못 찾아도 인터락을 끄지 않는다(연결할 때마다 다시 찾는다) · config import 시점(333행)에 실행돼 USB 포트를 훑고 print 한다 — config 를 import 하는 모든 도구에서 부수효과. 못 찾으면 인터록이 꺼진다(336행). |
| `camera_thread` | `zone_at_point` | 143 | ring 기본값이 해상도 배율 없는 VGA 값(config.HAND_ROI_RING_PX_VGA, 149행)이다 — 런타임은 536행에서 self._ring_px 를 넘겨 영향 없음. |
| `camera_thread` | `roi_at_point` | 153 | 호환용 래퍼 — Demo 안에서는 selftest/test_hoi_sim.py 만 부른다. 런타임은 zone_at_point 를 쓴다. |
| `camera_thread` | `CameraThread.set_active` | 247 | 호출처 없음(Demo 전체·test·selftest grep 0건). 그래서 _is_active 는 항상 True 이고 _process_frame 의 비활성 분기(478-481행)도 도달하지 않는다. |
| `camera_thread` | `CameraThread.set_draw_boxes` | 253 | ✅ 해결(③ R3 — 손 검출은 박스를 그리기 전 사본으로) · docstring(254행)은 「표시만 바뀐다」지만, 켜면 _process_frame 505행에서 YOLO 박스를 frame 에 그린 뒤 512행에서 그 frame 으로 손 검출을 돌린다 — 손 모델 입력이 표시 설정에 따라 달라진다. |
| `camera_thread` | `CameraThread.draw_boxes` | 258 | 런타임 호출처 없음 — selftest/test_console_flow.py 만 읽는다. |
| `camera_thread` | `CameraThread.set_host` | 285 | 호출처 없음(Demo 전체·test·selftest grep 0건). 호스트는 __init__ 의 CAMERA_TCP_HOST(.camera_ip) 로만 정해진다. |
| `camera_thread` | `CameraThread._undistort` | 313 | frame_orient.undistort(102-106행)와 같은 cv2.remap 을 복제했다(313-316행) — frame_orient 가 「단일 출처」라고 적어 둔 것과 어긋난다. |
| `camera_thread` | `CameraThread.run` | 371 | 백엔드 로드 로그가 import 시 print(51행)와 run 의 log_signal(376행)로 두 번 나간다. |
| `camera_thread` | `CameraThread._process_frame` | 467 | 손 검출이 YOLO 박스가 그려진 뒤의 frame 에서 돈다(505행 그리기 → 512행 detect). 오버레이 켬/끔·촬영 여부가 손 모델 입력을 바꾼다. 536행은 self._tracks 를 락 없이 읽는다. |
| `camera_thread` | `CameraThread._connect_tcp` | 548 | 수신 버퍼 1MB(555행)가 config 밖에 하드코딩돼 있다. |
| `gpio_input` | `GpioInputController.__init__` | 37 | 시작 시 EMO 핀이 HIGH 면 생성자 안에서 곧바로 on_button("EMO") 콜백을 부른다(78-80행) — 객체가 다 만들어지기 전에 호출부로 신호가 나간다. |
| `serial_ports` | `resolve` | 17 | 모듈 안 __main__(스크립트 실행, voice/시연절차.md)에서만 쓰인다 — config 는 ESP32_S3 상수만 import 한다. |
| `serial_ports` | `describe` | 30 | __main__ 스크립트 실행 때만 쓰인다(다른 import 없음). |
| `detector` | `PyTorchDetector.__init__` | 40 | config.INFERENCE_BACKEND="hailo"(config 103행)라 런타임에서 쓰이지 않는 경로. config 101행 주석은 여전히 pytorch 를 「현재」라 적고 있다. |
| `detector` | `PyTorchDetector.detect` | 50 | 현 설정(hailo)에선 미사용. |
| `detector` | `HailoDetector.__init__` | 81 | 클래스 이름표 {0:B1…4:EMO}가 103행에 하드코딩(.hef 메타 미사용). 클래스 docstring(76행)은 아직 「Phase B 검증 전 미검증」이라 적혀 있다. |
| `hailo_device` | `shutdown` | 89 | 호출처 없음(Demo 전체·test·selftest grep 0건 — detector.py:140 주석 언급뿐). 또 VDevice 를 release 하지 않고 참조만 None 으로 지운다(97-98행). |
| `hand_tracker` | `HandTracker.__init__` | 108 | 모델 파일명 palm_detection_lite.hef·hand_landmark_lite.hef 가 124-125행에 하드코딩(config 에는 폴더만 있음). |
| `hand_tracker` | `HandTracker.close` | 191 | 런타임 호출처 없음 — 이 파일 __main__(215행)에서만 부른다. NPU 자원을 해제하지 않고 참조만 지운다. |
| `frame_orient` | `undistort_map` | 87 | 런타임은 안 쓰고 test/tool_probe.py·test/capture_dataset.py 만 쓴다. |
| `frame_orient` | `_selftest` | 114 | 호출처 없음 — frame_orient.py 에 __main__ 블록이 없고 Demo 전체에서 부르는 곳이 없다(test/·selftest/ 의 _selftest 는 각자 다른 함수). |
| `tool_worker` | `main` | 77 | 고아 판정이 os.getppid()==1 뿐(96행) — 부모가 죽어 서브리퍼(systemd --user 등)에 입양되면 ppid 가 1이 아니어서 워커가 계속 돈다. |
| `tool_state` | `ToolState.update` | 72 | 메인 조건은 「-in-hand」 클래스(tool_v4 · tool_state.py 39-41행)를 전제하나 config.TOOL_MODEL_PATH=tool_v3.pt(config 400행)의 클래스는 driver·wrench·pliers 뿐이라 현 설정에선 손끝-박스 보조 조건만 작동한다. |
| `tool_state` | `ToolState._in_hand_tool` | 110 | 현 모델 tool_v3 에는 -in-hand 클래스가 없어 항상 None. |
| `fsm` | `SafetyFSM.update_vision` | 139 | 창 기반 누적(window_n 분기, 156-171행)은 폐기됐지만 코드가 남아 있다 — config.HAND_WINDOW_N=0 이라 기본 실행에선 도달하지 않는다. |
| `fsm` | `SafetyFSM.press_button` | 216 | ~~WARNING 상태에서 정답 버튼을 누르면 무시된다~~ — ✅ 해결 — 판정 수정 ①: 경고 해제 + 단계 완료(§4.1-6). |
| `recipe` | `_validate` | 33 | ~~누락 시 기본 1.0 · GUI KeyError 가능~~ — ✅ 해결 — 판정 수정 ①: 누락 시 config 값으로 채우고, recipe.json 과 config 가 같은지 `selftest/test_recipe.py` 가 검사한다(§4.1-9). |
| `interlock` | `InterlockController._open` | 98 | ✅ 2.0초는 해결(③ — `config.INTERLOCK_BOOT_WAIT_SEC`) · 잠금을 쥔 채 기다리는 것은 그대로 · 연결 성공 뒤 _lock 을 쥔 채 2초 sleep(110행) — 그동안 전송 스레드도 멈춘다. 2.0초는 config 밖 하드코딩. |
| `interlock` | `InterlockController.gave_up` | 168 | 호출처 없음 — precheck.py:50 은 카메라의 gave_up 만 읽는다. |
| `safety_console` | `SafetyConsole._init_ui` | 219 | settings_panel.closed 를 _toggle_settings(False) 에 두 번 연결한다(303·308행) — 닫을 때마다 두 번 불린다(무해하나 중복). |
| `safety_console` | `SafetyConsole._close_sheets` | 553 | 558행 `except_ is not self.log_browser` 검사는 호출부가 log_browser 를 넘기는 곳이 없어 항상 참이다(죽은 분기). |
| `safety_console` | `SafetyConsole._on_boxes_changed` | 778 | 표시 전용이 아니다 — 켜면 camera_thread.py:504-512 에서 버튼 박스·라벨이 그려진 frame 으로 손 검출(_hand.detect)이 돈다. 표시 설정이 손 검출 입력을 바꾼다(시연촬영 중엔 sink 때문에 항상 그려진다, 497행). |
| `safety_console` | `SafetyConsole._publish_state` | 1026 | 서브작업 공구를 레시피 원본(spec.get("tool"), 1047행)에서 읽어, 설정에서 바꾼 지정 공구(_tool_override)가 반영되지 않는다 — _sub_spec_for(1021-1022행)와 다르다. |
| `safety_console` | `SafetyConsole.keyPressEvent` | 1257 | 독스트링(1261행)은 「Full Screen 이라 메뉴가 유일한 출구」라 하나 main.py 는 전체화면을 쓰지 않는다(show/showMaximized/고정크기, main.py:28-29·52-55). |
| `safety_console` | `SafetyConsole._start_recording` | 1465 | 카메라 모드에서 첫 프레임이 없을 때 크기 폴백이 가로 (640, 480)으로 박혀 있다(1483행) — 회전(CCW90) 뒤 프레임은 세로라 config.DEMO_FPV_SIZE(480, 640)와 어긋난다. 독스트링은 「메뉴에서 켤 때만」이라 하나 194행이 RECORDING_ENABLED 이면 자동 시작한다(현재 config 값 False). |
| `overlay` | `_glow` | 74 | 빈 함수(L87 return). 2026-08-03 폐기 후 호출부(overlay.py L119·L574, overlay_menu.py L90) 유지용으로만 남음 — 사실상 죽은 코드. |
| `overlay` | `_Panel.apply_theme` | 101 | L118-119 에서 자식 QLabel 마다 no-op 인 _glow 를 부른다(무의미한 루프). |
| `overlay` | `StatusPanel.update_view` | 177 | 인자 sub_running 이 본문에서 전혀 쓰이지 않는다(L177). docstring 은 '진행 중'으로 표시한다고 하지만 동작 없음. 호출부(safety_console.py L1301)도 넘기지 않는다. |
| `overlay` | `GlowFrame.level` | 459 | 앱 코드에서는 읽지 않고 테스트(test_overlay.py·test_console_flow.py)만 사용. |
| `overlay` | `GlowFrame.relayout` | 462 | place() 와 달리 parent_rect 의 left/top 오프셋을 무시하고 x=pw*0.125, y=0 으로 고정(L465). 비율은 모듈 상수 VIDEO_*_RATIO. |
| `overlay` | `AlertBanner.show_block` | 539 | ✅ 해결(② G5 — EMO 차단은 따로 문구 · ③ `SafetyConsole._show_block_banner` 가 고른다) · reason 기본 문구가 하드코딩(L539); 유일한 호출부(safety_console.py L1317)가 인자 없이 부르므로 EMO(비상정지)로 BLOCK 이 돼도 '순서 위반이 계속되어 인터락이 작동했습니다'로 표시된다(같은 블록 L1307 주석은 EMO 는 순서 위반이 아니라고 함). |
| `overlay` | `ConnBar.relayout` | 660 | 위치 0.03/0.04 가 _POS 표(L32) 밖 리터럴(L661) — 모듈 docstring 의 '% 값은 _POS 사본' 규칙과 어긋남. |
| `overlay_menu` | `_Sheet.apply_theme` | 84 | L89-90 에서 no-op 인 _glow 를 라벨마다 부른다. |
| `overlay_menu` | `NotifyButton.__init__` | 115 | BADGE_BG/FG 색이 하드코딩(L112-113) — theme.py 로 색을 모은다는 overlay.py 규칙과 어긋남(테마 전환에 안 따라감). |
| `overlay_menu` | `MenuPanel.__init__` | 167 | self._free 루프가 range(0)(L199)이라 아무것도 안 만든다 — 남은 빈 칸 기능의 잔재. closed 시그널(L165)은 ✕ 버튼이 없어 emit 되지 않는데 safety_console.py L276 이 연결해 둠. |
| `overlay_menu` | `MenuPanel.apply_theme` | 221 | 항목 버튼 QSS 가 _row_button 의 것을 그대로 복사(L226-230). self._free 가 늘 비어 L235-237 루프는 무동작. |
| `overlay_menu` | `NotifyPanel.__init__` | 259 | closed 시그널(L257)은 ✕ 버튼이 없어 emit 되지 않지만 safety_console.py L285 가 연결해 둠. |
| `overlay_menu` | `NotifyPanel.push` | 287 | 알림 행이 무한히 쌓인다(삭제·상한 없음, 메모리 전용). |
| `overlay_menu` | `NotifyPanel.count` | 351 | 앱 코드에서는 쓰지 않고 테스트(test_overlay_menu.py·test_console_flow.py)만 사용. |
| `overlay_menu` | `SettingsPanel.__init__` | 379 | 공구 제목이 '4단계 지정 공구'로 하드코딩(L395)인데 recipe.json 의 wait_tool 단계는 2단계(B2 펌프/퍼지)다 — 화면 문구와 레시피 불일치. |
| `overlay_menu` | `CheckPanel.relayout` | 612 | 폭 0.34 리터럴(L613) — _SETTINGS_W(L44)와 같은 값을 따로 적음. |
| `overlay_menu` | `RecordPanel.set_save_dir` | 687 | self._save_dir 에 저장하지만 어디서도 다시 읽지 않는다(L689). |
| `overlay_menu` | `RecordPanel.set_state` | 692 | 'H.264(.mp4)로 저장 — 약 0.3MB/분'(L701) 하드코딩 — 코덱·확장자는 config.RECORDING_CODEC/EXT 가 정본이고 용량 수치 근거 없음. |
| `overlay_menu` | `RecordPanel.apply_theme` | 707 | 중지 버튼 글자색 #ffffff 하드코딩(L721). |
| `overlay_result` | `ResultPanel.apply_theme` | 164 | 판정 문구(_verdict, 85행)와 본문 항목(_section, 155·161행)은 show_result 때만 색을 칠해, 결과창이 열린 채 테마를 바꾸면 이 줄들은 옛 테마 색으로 남는다. |
| `anim` | `Pulse.stop` | 123 | TextPulse.stop 과 달리 멈춘 뒤 원래 QSS 로 되돌리지 않는다(anim.py:123-126). 호출부 overlay.AlertBanner.hide_all 이 곧바로 hide() 해서 드러나지 않는다. |
| `theme` | `text_qss` | 138 | 독스트링(139-140행)이 그림자는 QGraphicsDropShadowEffect 로 건다(overlay.py)고 하나, 그 방식(overlay._glow)은 2026-08-03 폐기돼 쓰지 않는다(overlay.py:74-80) — 낡은 설명. |
| `precheck` | `run_stage1` | 43 | 검출 모델 통과 문구 'console_v2 로드됨'(L58) 하드코딩 — 실제 모델은 config.HEF_MODEL_PATH 가 정본이라 모델을 바꾸면 어긋난다. |
| `precheck` | `run_stage2` | 82 | 앱에서 호출하지 않는다 — 호출처는 selftest/test_precheck.py 뿐(safety_console 은 run_stage1 만 사용). 영상 신선도 2.0초 하드코딩(L94), fps.fps_stale 기본값과 중복. |
| `precheck` | `_probe_hand` | 122 | run_stage2 에서만 불려 사실상 앱에서 안 쓰임. |
| `precheck` | `summary` | 137 | 앱 호출처 없음 — selftest/test_precheck.py 만 사용. |
| `session_stats` | `SessionStats.button_pressed` | 52 | 인자 expected 는 쓰이지 않는다(L52-54). 오답 눌림은 기록하지 않음. |
| `session_stats` | `SessionStats.sub_done` | 79 | 빈 함수(pass, L80)인데 safety_console.py L1158 에서 계속 호출된다. |
| `session_stats` | `SessionStats.frame` | 107 | 이름만 넘기면 신뢰도 0.0 을 표본으로 더한다(L116) — None 은 제외하는 규칙과 달리 평균을 끌어내림. 단 실제 호출(safety_console.py L842)은 늘 튜플이라 이 분기는 미사용. |
| `session_stats` | `SessionStats.finish` | 125 | 부작용: _started=None 으로 running 을 끈다(L150). 두 번 부르면 시작시각·총시간이 '지금' 기준으로 바뀐다. |
| `fps` | `fps_stale` | 29 | 임계 2.0초가 precheck.py:94 에 별도 숫자로 하드코딩돼 있다(docstring 이 같은 값을 유지하라고만 경고). |
| `state_publisher` | `StatePublisher.clear` | 60 | self._ok 가 False(폴더 생성 실패)여도 삭제를 시도한다 — 예외는 삼키므로 무해. |
| `demo_recorder` | `keep_screen_awake` | 25 | docstring(32행)에 「1인칭·3인칭은 멀쩡」이라는 3인칭 언급이 남아 있다 — 3인칭 웹캠은 2026-09-23 제거됨. |
| `demo_recorder` | `DemoRecorder.__init__` | 44 | _fpv_size 는 여기서 초기화되지 않고 start()(79행)에서 처음 생긴다. |
| `demo_recorder` | `DemoRecorder._feed` | 103 | 파이프 쓰기가 실패해 break 해도 _pushed 는 1 증가한다(125행). 파이프가 2개가 아니면(116행) 아무 경고 없이 프레임을 안 민다. |
| `demo_ffmpeg` | `FfmpegSet.start` | 62 | fpv 두 벌 중 하나가 즉시 죽으면 fpv_pipes 가 1개로 줄어(107-108행), demo_recorder._feed 의 len(pipes)==2 조건(demo_recorder.py:116) 때문에 남은 한 벌에도 프레임이 전혀 안 들어간다. |
| `voice_assistant` | `AudioLog.close` | 159 | ✅ 해결(③ V4 — `open_audio_log` 가 종료 때 부르도록 등록) · 호출하는 곳이 없다 — run() 은 무한 루프이고 once 로 return 할 때도 alog.close() 를 부르지 않는다(마이크_전체.wav 헤더가 마무리 안 될 수 있음). |
| `voice_assistant` | `esp_ip` | 165 | .camera_ip 경로(44행)가 config._CAMERA_IP_FILE(config.py:277)과 중복 정의돼 있다. |
| `voice_assistant` | `wav_payload` | 208 | 체크섬·프레임 구성이 voice_tts.frame(voice_tts.py:49-59)과 같은 코드로 중복돼 있다(주석상 의도적). |
| `voice_assistant` | `Speaker.__init__` | 250 | ip 를 생성 때 한 번만 받는다(380행) — 마이크는 재연결마다 주소 파일을 다시 읽지만 스피커는 통신경로 폴백을 따라가지 않는다. |
| `voice_assistant` | `run` | 362 | 모듈 docstring(13행)은 「실HW 에서 아직 돌아본 적이 없다(2026-09-06)」인데 본문 주석들은 2026-09-07 실측을 인용해 낡았다. |
| `voice_assistant` | `main` | 574 | --ip 도움말(576행)은 기본값이 Demo/.camera_ip 라 하지만 실제로는 .audio_ip 를 먼저 본다(esp_ip, 171행). |
| `voice_lib` | `is_wake` | 50 | docstring·주석은 「앞 두 글자 접두 매칭」이라 하지만 실제로는 문장 어디든 포함 여부(53행 `w in n`)를 보며, 목록에 3글자 「가지언」「아디언」도 있다. |
| `voice_lib` | `read_tool_dets` | 190 | 기본 경로 TOOL_SHM(41행 "/dev/shm/sop_tool/resp.json")이 config.TOOL_SHM_DIR(config.py:402)을 하드코딩 중복. docstring 의 camera_thread.py:242 인용(193행)은 낡은 행 번호(현재 해당 로직은 약 229행). |
| `voice_card` | `_seen_tool` | 66 | voice_lib.answer_key(voice_lib.py:212-225)와 같은 최고점 공구 선택 로직의 중복 — 27행 주석상 의도적으로 따로 둠. |
| `voice_tts` | `frame` | 49 | voice_assistant.wav_payload(voice_assistant.py:208-217)와 같은 체크섬 코드의 중복(docstring 이 일치 필요를 명시). |
